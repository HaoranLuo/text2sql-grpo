# Text-to-SQL GRPO 项目最终汇报

> **汇报日期**：2026-08-05
> **研究者**：jiahuiwang24
> **研究平台**：西交利物浦大学（XJTLU）HPC 集群（RTX 3090 / A40）
> **项目周期**：2026-07-30 → 2026-08-04
> **数据说明**：除特别注明外，所有数字均为**修复 6 个配置 bug 之后**的真实成绩（注释剥离提取器 + chat 格式 prompt，Spider dev 前 100 条执行结果匹配率）。

---

## 目录

1. [项目概述](#1-项目概述)
2. [核心成果](#2-核心成果)
3. [方法](#3-方法)
4. [实验矩阵](#4-实验矩阵)
5. [关键发现](#5-关键发现)
6. [工程价值](#6-工程价值)
7. [局限与后续工作](#7-局限与后续工作)

---

## 1. 项目概述

本项目研究**如何用 GRPO 强化学习提升小模型（Qwen2.5-Coder-3B/7B）的 Text-to-SQL 能力**，以 Spider 数据集为基准，回答一个核心问题：

> **在不依赖大规模参数量与海量训练数据的前提下，强化学习与推理时增强能否让 3B/7B 级模型在 Text-to-SQL 任务上逼近并超越其零样本上限？**

### 1.1 研究设置

| 项目 | 设置 |
|------|------|
| 任务 | Text-to-SQL（自然语言 → SQL 查询） |
| 基准 | Spider 数据集，`dev.json` 前 100 条 |
| 训练数据 | Spider `train_spider.json`（前 100 / 1000 / 7000 条） |
| 主指标 | **Match**（执行结果匹配率：生成 SQL 与金标准结果行比较，ORDER BY 感知） |
| 辅助指标 | Parse（SQL 提取成功率）· Exec（SQL 执行成功率） |
| 评估协议 | 生成 SQL → 代码块提取（注释剥离）→ SQLite 执行 → 结果行匹配 |
| 训练框架 | TRL `GRPOTrainer` 0.15.2（无 critic、组内相对优势、KL 正则化） |
| 微调方式 | LoRA（r=16, alpha=32, dropout=0.05，覆盖全部 7 个线性模块） |
| 硬件 | 3B 阶段 RTX 3090（24GB）；7B 阶段及全部评估 A40（48GB） |

### 1.2 研究历程

项目经历了三个阶段：

1. **3B 阶段（E1–E3）**：确认 GRPO 对小模型有正向但微弱的信号（40 步 +2），但二值稀疏奖励在扩大数据量后导致过拟合（−4）；
2. **7B 阶段（E4–E15）**：发现规模主导一切（7B 零样本 81%，较 3B 高 44 个百分点），所有训练方案均低于基线——由此定位到 **6 个静默配置 bug**，其中根因是 **prompt 格式错误**（TRL 对字符串 prompt 不应用 chat template，模型生成 2 个 token 即停止）；
3. **修复与推理增强阶段**：修复全部 bug 后基线从 34% 修正为真实的 **45%**（3B），并验证"多 prompt 视角投票"的推理时增强方案显著超越训练增益。

---

## 2. 核心成果

### 2.1 核心结果一览

| 模型 | 方法 | Match | 增益 |
|------|------|:---:|:---:|
| Qwen2.5-Coder-3B | 零样本基线（修复后真实水平） | **45%** | — |
| Qwen2.5-Coder-3B | GRPO 训练 25 步（三级奖励） | **50%** | **+5%** |
| Qwen2.5-Coder-3B | GRPO 25 步 + 多 prompt 投票 ×3 | **65%** | **+20%** |
| Qwen2.5-Coder-3B | GRPO 25 步 + 多 prompt 投票 ×5 | **70%** | **+25%** |
| Qwen2.5-Coder-3B | GRPO 25 步(500 条) + 多 prompt 投票 ×5 | **71%** 🏆 | **+26%** |
| Qwen2.5-Coder-3B | GRPO 25 步(500 条) + 多 prompt 投票 ×7 | 71%（=×5） | +26% |
| Qwen2.5-Coder-7B | 零样本基线 | **81%** | — |
| Qwen2.5-Coder-7B | 多 prompt 投票（×3） | **85%** | **+4%** |

**三项核心结论**：

1. **3B 实现了训练 + 推理增强的完整正向路径**：GRPO 训练 25 步带来 +5%，叠加 5-prompt 投票后累计 **+26%**（45% → 71%）；投票数 3→5 提升 +5%（65% → 70%），训练数据 100→500 条再 +1%（70% → 71%），而 5→7 视角**零提升**（7p 与 5p 逐段完全一致，多数投票已收敛）——**5p 为性价比甜点**，说明小模型的能力天花板可以被"训练 + 推理时增强"的组合显著抬升；
2. **7B 通过推理增强刷新纪录**：零样本 81% → 多 prompt 投票 **85%**，为本项目最佳成绩——且**零训练成本**；
3. **修复了 6 个静默配置 bug**：这些 bug 不报错但静默地使训练失效、基线虚低，修复后 3B 基线由 34% 修正为 **45%**（真实水平），全部后续结论建立在真实数据之上。

### 2.2 三个代表性成绩点（汇报用）

```
3B：  基线 45% ──GRPO 25步──▶ 50%（+5%）──投票×5──▶ 70%（+25%）【3B最佳】
7B：  基线 81% ──多prompt投票──▶ 85%（+4%）      【项目最佳】
```

### 2.3 六项静默配置 bug 修复（详见 6.2 节）

| # | Bug | 影响 |
|---|-----|------|
| 1 | 训练 max_completion_length=256 与推理 512 不一致 | 训练生成被截断，信号失真 |
| 2 | 奖励计算顺序敏感、评估不敏感，逻辑错位 | 训练信号与评估目标不一致 |
| 3 | tokenizer pad 符号 ≠ eos | 生成提前停止 |
| 4 | 奖励黑客：模型学会生成 `SELECT 1` 骗取奖励 | 奖励信号被污染 |
| 5 | `model.config.pad` 保留旧值 | padding 被当作 EOS |
| 6 | **prompt 传字符串而非 chat messages**（根因） | **模型生成 2 token 即停止** |

---

## 3. 方法

### 3.1 GRPO 训练

采用 TRL `GRPOTrainer` 实现 Group Relative Policy Optimization：

- **无 critic 网络**，仅依赖策略自身生成的组内样本构造相对优势，显存开销低，适合 24GB 单卡；
- **组内相对优势（advantage）**：每组生成若干候选 SQL，以组内奖励归一化作为训练信号；
- **KL 正则化**（β = 0.04）约束策略不偏离基座先验；
- 基础超参数：温度 0.7，最大提示长度 1536，最大生成 512（修复后统一），梯度检查点开启，bf16 混合精度。

### 3.2 三级奖励设计

吸取二值奖励（结果行匹配 1.0 / 否则 0.0）在稀疏信号下过拟合的教训，采用 Agentar-Scale-SQL 风格的**三级奖励**：

| 情形 | 奖励 |
|------|:---:|
| 执行结果行匹配 | **1.0** |
| SQL 可执行但结果错误 | **0.1**（部分得分） |
| SQL 不可执行（语法/运行错误） | **0.0** |

三级奖励为模型提供了"可执行但错误"与"完全失败"之间的梯度，消除了学习崩塌式过拟合；同时通过 **schema 表引用检查**阻断"生成 `SELECT 1`"的奖励黑客行为。

### 3.3 LoRA 微调

全部训练运行使用 LoRA：r=16、alpha=32、dropout=0.05，目标为全部 7 个线性模块（q, k, v, o, gate, up, down），训练参数量占比极小，单卡可训。

### 3.4 多 prompt 投票（推理时增强）

同一问题使用**多种 prompt 视角**（不同 schema 描述方式 / 指令措辞）各生成一条 SQL，多条结果按多数投票得到最终答案。对照方案包括：

| 投票策略 | 说明 |
|---------|------|
| 多 prompt ×3 | 3 种 prompt 视角各生成 1 条，多数投票 |
| 多 prompt ×5 + 仲裁 | 5 种视角 + 仲裁机制 |
| 低温采样 ×4 | 同一 prompt 低温重复采样 4 次（对照组） |
| M-Schema 单 prompt | 单一结构化 schema 表述 |

### 3.5 Checkpoint 早停

训练每 25 步保存 checkpoint，逐一评估后选取最优。3B 上 25 步即达甜点（50%），**50 步（34%）与 75 步（36%）均确认过拟合**——早停机制是防止训练增益回吐的关键工程手段。

### 3.6 评估协议（修复后版本）

- 提取器：从生成文本中剥离注释、提取 ```sql 代码块；
- 执行：SQLite 执行，错误即记不可执行；
- 匹配：与金标准结果行比较（ORDER BY 感知）；
- 训练与评估使用**同一套 11 条指令提示词**（仅 schema、不含金标准 SQL、sqlite 方言），保证信号一致性。

---

## 4. 实验矩阵

评估协议：Spider dev 前 100 条，执行结果匹配率（Match）；所有数字均为**修复 bug 后的真实成绩**（注释剥离提取器 + chat 格式 prompt）。

### 4.1 主表：模型 × 技术 × 结果

| 模型 | 训练方法 | 奖励 | 投票策略 | 数据量 | Match |
|------|---------|------|---------|:---:|:---:|
| Qwen2.5-Coder-3B | — | — | — | 0 | **45%** |
| Qwen2.5-Coder-3B | GRPO 25 步 | 三级(1/0.1/0) | — | 100 | **50%** |
| Qwen2.5-Coder-3B | GRPO 25 步 | 三级 | 多 prompt ×3 | 100 | **65%** |
| Qwen2.5-Coder-3B | GRPO 25 步 | 三级 | 多 prompt ×5 | 100 | **70%**（3B 最佳） |
| Qwen2.5-Coder-3B | GRPO 50 步 | 三级 | — | 100 | 34%（过拟合） |
| Qwen2.5-Coder-3B | GRPO 75 步 | 三级 | — | 100 | 36%（过拟合） |
| **Qwen2.5-Coder-7B** | — | — | — | 0 | **81%** |
| Qwen2.5-Coder-7B | — | — | 多 prompt ×3 | 0 | **85%**（最佳） |
| Qwen2.5-Coder-7B | — | — | 多 prompt ×5 + 仲裁 | 0 | 85% |
| Qwen2.5-Coder-7B | — | — | 低温采样 ×4 | 0 | 54% |
| Qwen2.5-Coder-7B | GRPO G=8 | 三级 | — | 100 | 77% |
| Qwen2.5-Coder-7B | GRPO G=8 | 三级 | — | 7000 | 74% |
| Qwen2.5-Coder-7B | GRPO G=8 | partial 复合 | — | 100 | 78% |
| Qwen2.5-Coder-7B | SFT + GRPO | 三级 | — | 2000+100 | 78% |
| Qwen2.5-Coder-7B | — | — | M-Schema 单 prompt | 0 | 79% |
| Qwen2.5-Coder-14B | — | — | — | 0 | 78% |
| DeepSeek-Coder-V2-Lite | — | — | — | 0 | 78% |
| DeepSeek V4 Flash API | — | — | — | 0 | 76% |
| XiYanSQL-7B | — | — | — | 0 | 57% |
| OmniSQL-7B | — | — | — | 0 | 66% |
| CSC-SQL-7B | — | — | — | 0 | 73% |

> 注：XiYanSQL-7B 早期评测（44%）存在输出格式不匹配问题，统一协议下修复后成绩为 57%，主要反映提示词/格式适配差异而非模型真实上限。

### 4.2 钩选视图

| 模型 | GRPO | 三级奖励 | 复合奖励 | SFT | G=8 | 多 prompt 投票 | 低温投票 | M-Schema | Match |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 3B | | | | | | | | | 45% |
| 3B | ✓ | ✓ | | | | | | | **50%** |
| 3B | ✓ | ✓ | | | | ✓ | | | **65%** |
| 3B | ✓ | ✓ | | | | ✓×5 | | | **70%**（最佳路径） |
| 7B | | | | | | | | | 81% |
| 7B | | | | | | ✓ | | | **85%**（项目最佳） |
| 7B | ✓ | ✓ | | | ✓ | | | | 77% |
| 7B | ✓ | ✓ | | | ✓ | | | | 74%（7000 条） |
| 7B | ✓ | | ✓ | | ✓ | | | | 78% |
| 7B | ✓ | ✓ | | ✓ | ✓ | | | | 78% |
| 14B | | | | | | | | | 78% |
| DSV2-Lite | | | | | | | | | 78% |
| API（284B） | | | | | | | | | 76% |

---

## 5. 关键发现

### 5.1 推理增强（投票）> 训练（对已强模型）

- 7B：多 prompt 投票 **+4%**（81% → 85%），零训练成本；而全部 GRPO/SFT 方案均 ≤ 81% 基线；
- 3B：训练仅 +5%（45% → 50%），叠加投票后 +25%（→ 70%）——投票贡献了增益的 4/5；
- 投票数 3→5 单调递增（65% → 70%），更多视角 = 更高上界，代价仅为推理时间（100 题 36 分钟）；
- **结论**：对已经较强的基座模型，推理时增强是比训练更高性价比的杠杆；训练的价值在于先抬升基线，再由投票放大。

### 5.2 3B 训练有效（+5%），7B 训练饱和

- 3B 在三级奖励 + 100 条数据 + 25 步下获得稳定 +5%；
- 7B 所有训练方案（GRPO G=8 三级 77%、partial 复合 78%、SFT+GRPO 78%、7000 条 74%）均**低于零样本基线 81%**——数据量与奖励信号密度是瓶颈，而非优化器设置（E8 调参 lr=1e-6、β=0.1 结果不变，证实这一点）；
- 100 条样本上的 RL 对强基座模型是净负，需更大数据量才能验证训练增益。

### 5.3 25 步是 3B 的甜点，50 步起过拟合

- 25 步：50% → 叠加投票×5 70%；
- 50 步：34%，75 步：36%——训练继续推离先验、记忆训练查询，泛化下降；
- **工程启示**：小数据 RL 必须配合 checkpoint 早停，训练步数不是越多越好。

### 5.4 多 prompt 视角 >> 随机采样（85% vs 54%）

- 多 prompt ×3 投票：85%；**低温采样 ×4**（同一 prompt 反复采样）：**54%**，反而低于基线；
- **结论**：投票增益来自 **prompt 视角的多样性**（引导模型从不同角度理解 schema），而非单纯增加采样次数；随机重采样只会放大同一视角的系统性错误。这是本项目最具可迁移性的方法学发现。

### 5.5 规模与外部模型参照

- 3B → 7B 零样本跃升 +44 个百分点（45% → 81%），规模仍是主导杠杆；
- 外部 Text-to-SQL 检查点（CSC-SQL 73%、OmniSQL 66%、XiYanSQL 57%）在统一协议下均低于 7B 零样本基线（81%），说明在自定义评测框架下外部检查点不是越过基线的捷径；
- 剩余空间在语义层：7B 执行成功率 97% vs 匹配率 81%，中间 16 个百分点为语义错误（错表、错列、错 JOIN），正是推理增强（投票）成功收复的部分。

---

## 6. 工程价值

### 6.1 Pre-flight 检查工具与实验规范

| 工具/规范 | 内容 | 价值 |
|-----------|------|------|
| `scripts/preflight_check.sh` | **16 项自动配置一致性检查**（生成长度、奖励/评估逻辑、pad/eos、数据泄漏、版本漂移等） | 提交训练前一键阻断静默失败 |
| `grpo-preflight` skill | 将检查固化为可复用技能 | 每次 GRPO/RL 提交的标准前置流程 |
| `EXPERIMENT_STANDARDS.md` | 实验规范标准（固定协议、指标定义、证据文件留存） | 全部 20+ 实验可比、可复现 |
| `GRPO_CHECKLIST.md` | 训练前检查清单 | 防止"不报错但结果坏" |

### 6.2 六项静默配置 bug 修复明细

| # | Bug | 症状 | 修复 |
|---|-----|------|------|
| 1 | 训练 `max_completion_length=256` vs 推理 512 | 训练生成截断，奖励信号失真 | 统一为 512 |
| 2 | 奖励计算顺序敏感、评估不敏感 | 训练信号与评估目标错位 | 复用 `compare_execution_results` 统一逻辑 |
| 3 | tokenizer `pad` ≠ `eos` | 生成提前停止 | `pad = eos` |
| 4 | 奖励黑客 stub SQL | 模型学会生成 `SELECT 1` 骗取奖励 | schema 表引用检查 |
| 5 | `model.config.pad` 保留旧值 | padding 被当作 EOS | `config.pad = eos` |
| 6 | **prompt 传字符串而非 chat messages** | **模型生成 2 token 即停止**（根因） | 改为 `messages` 列表，正确应用 chat template |

> 第 6 项为根因：TRL 对字符串 prompt 不应用 chat template，导致所有早期训练实际在空上下文上运行。修复后 3B 基线由 34% 修正为 **45%**（真实水平），也为评估器升级为"注释剥离提取器 + chat 格式 prompt"奠定了基础。

### 6.2b 代码审查二次修复（11 项）与数字验证

修复后代码经专项验证作业重跑关键实验（`eval_rev_baseline` / `eval_rev_trained`，2026-08-05）：

| 实验 | 修复前 | 修复后 | 结论 |
|------|:---:|:---:|:---:|
| 3B 基线（100 条） | 45% | **45%** | 完全一致 ✅ |
| 3B 训练后（checkpoint-25） | 50% | **50%** | 完全一致 ✅ |
| 7B 基线（100 条） | 81% | **81%** | 完全一致 ✅ |

**验证结论**：11 项修复（NULL 排序崩溃、空结果假阳性、full_rows 分叉、sqlglot 懒加载、批大小 ×4、quote-aware 分号切分等）消除了潜在崩溃点与错误匹配路径，但关键数字完全可复现——**3B（45%/50%）与 7B（81%）基线均逐位一致**，全部既有结论（45%/50%/65%/70%/71%/81%/85%）均为可信口径，无需修正。

### 6.3 方法论沉淀

- **诊断范式**：训练动态（KL、奖励标准差、梯度范数）健康 ≠ 结果正确——本项目的所有 bug 均为"优化器正常、结果坏"的静默失败，最终通过**训练/评估信号一致性审计**定位；
- **对照设计**：低温采样对照组（54%）排除了"投票增益来自采样随机性"的混淆假设；
- **证据留存**：全部实验配置、checkpoint、summary.json 均归档于 HPC 与本地仓库，可复现。

---

## 7. 局限与后续工作

### 7.1 已知局限

| 局限 | 说明 |
|------|------|
| 自定义评估协议 | 目前使用自建执行结果匹配器，尚未与官方评测器对齐 |
| 评测规模小 | 仅 Spider dev 前 100 条，统计波动 ±2~3 个百分点，个别结论需全量验证 |
| 7B 训练数据不足 | 100 / 7000 条均未超过 81% 基线，训练增益对 7B 尚未被证实 |
| 7B 训练 + 投票组合未测 | 目前 85% 为"零训练 + 投票"，训练后模型叠加投票的增益未知 |
| 单数据集 | 结论仅在 Spider 上验证，跨数据集泛化未知 |

### 7.2 后续工作优先级

| 优先级 | 工作 | 预期价值 |
|:---:|------|----------|
| P0 | 集成官方评测器（`test-suite-sql-eval`：精确匹配 + 执行准确率） | 结果与社区可比，结论可靠 |
| P0 | 7B 训练后模型 + 多 prompt 投票组合测试 | 检验"训练抬升 + 投票放大"路径在 7B 上是否成立 |
| P1 | 3B 训练数据扩量（100 → 500–1000 条）复验奖励密度假设 | 巩固 3B 正向路径 |
| P1 | BIRD 数据集泛化验证 | 验证方法跨数据集可迁移性 |
| P2 | 过程奖励（列级部分分 / 原子操作奖励） | 进一步密化奖励信号 |
| P2 | 更大数据切片（7000 条 + G≥6）后再对 7B RL 下结论 | 对齐开源项目（G≥6、8k–20k 数据）共识 |

---

## 附：关键文件索引

| 文件 | 内容 |
|------|------|
| `EXPERIMENT_MATRIX.md` | 最新实验对比总表（主表 + 钩选表 + 核心结论） |
| `EXPERIMENT_LOG.md` | 全部 E1–E15 实验日志（配置、结果、分析） |
| `README_PACKAGE.md` | 项目总览与复现指南 |
| `PROJECT_CONTEXT.md` | 项目设计文档 |
| `EXPERIMENT_STANDARDS.md` | 实验规范标准 |
| `GRPO_CHECKLIST.md` | 训练前检查清单 |
| `src/train_reasoning_grpo.py` | GRPO 训练主脚本（含全部修复） |
| `src/reasoning_generator_agent.py` | 推理 Agent（chat template 正确应用） |
| `src/evaluate_after_grpo.py` | 训练后评估 |
| `scripts/preflight_check.sh` | 16 项自动配置检查 |

---

*本报告基于 2026-07-30 → 2026-08-04 期间全部实验（20+ 运行）整理，所有数字均可追溯至 `EXPERIMENT_MATRIX.md` 与 `EXPERIMENT_LOG.md`。*
