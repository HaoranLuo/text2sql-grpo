# 执行编排计划（草案 v1.2·授权实测版，待批准）

> 2026-08-13 制定；v1.1 按"最快速度"重排；**v1.2 按 HPC 授权实测修正并行上限**。
> 基于 IMPROVEMENT_PLAN（P0-P2 十项）+ PROJECT_GUIDE 状态 + PAPER_PLAN 分层。
> **范围**：实验推进、监管检查、记录推送、决策支持。**排除**：写论文（L1 技术报告、L4/L5/L7/L8 写作）。
> **红线**：① 花 GPU/API 的动作必须报方案 + 用户批准后才执行；② 改实验协议（奖励/数据划分）必须先过 preflight + 用户确认；③ 只读动作（查日志/核对/分析/记录）主控自主做。

---

## 〇、HPC 授权实测（2026-08-13，sacctmgr 只读查询，权威）

**用户 jiahuiwang24（账户 binchen02…）的 QOS 授权**：

| QOS | 资源 | 同时在跑 | 可排队 | 墙钟 | 用途 |
|---|---|---|---|---|---|
| 1a40 | 1×A40 | 1 作业 | **8 个** | 7 天 | 主分区长作业（训练/全量评估） |
| gpudebug | 1×3090 | 1 作业 | 1 个 | **1 小时** | 短作业（探针/切片评估） |
| 32cores / cpudebug | 纯 CPU | — | — | — | 预处理（无 GPU 任务用不上） |

**硬约束**：
1. 每个 GPU QOS 每用户同时只 1 卡 1 作业；无 2a40/4a40/8a40 授权 → 单作业多卡、多训练并行均不可行
2. **极限并行 = 2 卡**：1×A40（1a40 长作业）+ 1×3090（gpudebug，≤1h 短作业），两 QOS 独立计算可同时跑
3. **提速主力 = 批量入队**：1a40 可排 8 个作业，调度器自动衔接，消除人工等待
4. gpudebug 1 小时墙钟：评估作业必须预估时长，超 1h 的拆片续跑（`finer_port/eval_*.py` 支持 --start-index/--limit）

---

## 一、并行策略（按授权现实，v1.2）

1. **"1+1"双卡搭配**：A40 跑训练长作业；gpudebug 见缝插针跑 ≤1h 短作业（探针/切片评估/短消融）
2. **批量入队（提速主力）**：所有作业**提前做完 preflight**，按依赖顺序一次性批量提交进 1a40 队列（8 槽），作业间自动衔接
3. **零 GPU 任务并行**：Phase 0 三路（T0.2/T0.4/T0.5）同时派 agent
4. **API 备料并行**：Phase 5 蒸馏数据生成（DeepSeek API）在训练期间同步跑（费用 D5 批准）

**新瓶颈与对策**：GPU 排队吞吐固定；监管核对必须跟得上 → 出数即核对，核对优先于下一提交；建「作业登记板」供监控 cron 盯梢。

---

## 二、阶段总览（授权现实版）

```
Phase 0 准备（当天，零 GPU，三路并行）
  T0.1 链路确认（前置 5 分钟）
  三路并行：T0.2 数据卫生 ∥ T0.4 MPEV 65% 分析 ∥ T0.5 MAC-SQL 获取
  T0.3 SFT 配方定稿 + preflight（依赖 T0.2）
        │ 准备完成即解锁
        ▼
GPU 双卡轨道（批量入队 + 长短搭配）
  A40 队列（1a40，批量入队自动衔接）：
    [1] Phase 1 蒸馏 SFT ──→ [2] Phase 2 T2.1 三级 ──→ [3] Phase 2 T2.2 四分量 ──→ [4] Phase 5 大规模 GRPO
  3090 插队（gpudebug，≤1h 切片）：
    Phase 4 FINER 探针（切片）∥ Phase 3 MPEV 官方验证（切片续跑）∥ 各 checkpoint 短评估
  API 并行：Phase 5 蒸馏备料（费用批准后启动）
        │ 全部结果到齐
        ▼
（后续：深度路线 L7/L8 实验，另行立项）
```

**时间线（授权现实估计）**：Phase 0 当天 → Phase 1 约 1 天 → Phase 2 两配方排队衔接约 2 天 → Phase 5 出数约 3-4 天。**总约 1.5-2 周到 Phase 5 出数**（批量入队消除人工等待是主要提速；相比原串行 2-3 周仍明显压缩）。GPU 作业 8-12 个，在预算内。

---

## 三、任务明细

### Phase 0：准备（当天，零 GPU）

| ID | 任务 | 内容 | 形态 |
|---|---|---|---|
| T0.1 | 链路确认 | gf-bastion + cpolar + `ssh hpc "squeue"` | 主控单步 |
| T0.2 | 数据卫生 P0#3 | 5.5k/1.5k 不相交划分 + sha256 + 1000 条蒸馏来源核查 | 并行 agent 1 + 监管 workflow |
| T0.3 | SFT 配方定稿 | train_sft.py 参数（D1）+ preflight --strict | 主控（依赖 T0.2） |
| T0.4 | MPEV 官方 65% 分析 | vav_self 89% / 同口径 75% / 官方 65% 差距归因 | 并行 agent 2（动态排查） |
| T0.5 | MAC-SQL 评估器获取 | 找 evaluation_spider.py 并 diff | 并行 agent 3（WebSearch/WebFetch） |

### Phase 1-4：GPU 双卡轨道（批量入队）

| 卡/槽 | 任务 | 内容 | Gate/决策 |
|---|---|---|---|
| A40[1] | **Phase 1 蒸馏 SFT** | 1000 蒸馏 + gold 15%（D1）；preflight --strict；全量 1034 官方+自定义双报 | **Gate G2**：单次 ≥55% → 进 GRPO；<55% → 诊断 1 轮重试（上限 2 次）→ D2 |
| 3090 | **Phase 4 FINER 探针** | FINER 官方权重跑 M1 探针（>1h 则拆片），归因 5.9pp | 与 SFT 训练并行 |
| 3090 | **Phase 3 MPEV 官方验证** | T0.4 归因修正后官方口径复测（切片续跑）；失败类型感知修复（P1#5） | **Gate**：官方 ≥5p+2pp → 采用；否则回退 5p → D4 |
| A40[2] | **Phase 2 T2.1 GRPO-三级** | 三级 1.0/0.1/0.0，SFT 检查点起，25-40 步 + std 触零即停 | 决策 D3 |
| A40[3] | **Phase 2 T2.2 GRPO-四分量** | 四分量 + dapo + 静默组过滤 + scale_rewards=None + β 0.01-0.02 | 排队自动衔接 T2.1 |

### Phase 5：大规模 GRPO（排队串行 + API 并行备料）

- 全量 8659 不相交 + G16/G32 对照 + 四分量 + 梯度修复 + β sweep + checkpoint 早停
- β sweep 无多卡并行 → **排队批量提交**（1a40 8 槽自动衔接）
- 蒸馏备料在 Phase 1-2 期间 API 并行生成（D5 批准后）
- 预期单次 68-76%；立项前决策 D7

### Phase 6：可选项目（按决策立项）

| 项目 | 内容 | 成本 |
|---|---|---|
| P2#9 Qwen3 A/B | 30B-A3B 零训练推理 / 4B-Base 训练线；严禁通用 Qwen3-8B | 1-2 作业 |
| P1#7 合成数据增强 | SQL-PaLM 式双验证 | API + GPU |
| P2#10 ORM 验证器 | 7B 验证器 + 3B 生成器 | GPU |
| P1#6 工程包 | vLLM V1 前缀缓存 + rlprobe + cron 恢复 | 工程 |
| BIRD 迁移 | 下月；前置 SAR-Agent 标注筛查 | API + GPU |

**明确不做**：PRM、多智能体 RL、投机解码、Spider 2.0、LLM-judge 奖励；裸 GRPO、3B 自蒸馏、低质跨域合成数据（三大约束仅可作对照）。

---

## 四、监管检查点（自进化闭环）

1. **训练提交前（强制）**：`grpo-preflight` + `preflight_check.sh --strict` 退出码 0 → 监管 workflow（多 reviewer 并行审 + 对抗验证）→ 才入队
2. **作业登记板**：批量入队后登记「作业→卡→预计时长→盯梢点」，监控 cron 按板盯梢（gpudebug 作业重点盯 1h 墙钟）
3. **评估出数后**：交叉核对 workflow（items 恒 1034 行 / pred-gold 对齐 / 数字与 records 一致）——**核对优先于下一提交**
4. **每实验闭环**：审查 → 修 P0/P1 → 复验 → record_experiment.py → git commit+push → PROJECT_GUIDE §7.3 更新
5. **每阶段末**：全量状态核对 workflow + 阶段报告

---

## 五、决策点清单（D0-D7）

| ID | 时机 | 决策 | 我的建议 |
|---|---|---|---|
| **D0** | 现在 | 批准 v1.2 授权实测版 | 批准；Phase 0 当天三路并行启动 |
| **D1** | T0.3 | SFT 配方参数 | LoRA r=32、lr 1e-5、3 epochs + dev 早停、gold 15% |
| **D2** | Gate G2 | SFT 单次 ≥55%？ | 是 → GRPO；否 → 诊断 1 轮重试（上限 2 次），再不行暂停报你 |
| **D3** | Phase 2 | GRPO 配方 | T2.1 三级先跑 → T2.2 四分量排队自动衔接（无多卡并行，串行对照） |
| **D4** | Phase 3 | MPEV 官方 ≥5p+2pp？ | 是 → 采用；否 → 回退 5p |
| **D5** | Phase 1 启动时 | API 并行备料费用 | 先 50 题实测账单（半天）→ 报上限 → 你批 → 训练期间并行生成 |
| **D6** | Phase 2 后 | Qwen3 A/B 立项 | 主战役告一段落后评估 |
| **D7** | Phase 2 后 | 大规模 GRPO 立项 | Phase 2 有增益 + T0.2 完成 → 立项 |
| **D8** | Phase 2 前 | 文献弹药选择(见 §七) | 建议:先零训练诊断与投票实验(插队推理);训练侧先 QAE A/B 后奖励栈 v2;judge/Verbal RL 暂缓 |

---

## 六、风险与约束

1. **并行上限已定死 = 2 卡（1 长 + 1 短）**：授权实测完毕，无变数；提速靠批量入队（8 槽）消除人工等待
2. **gpudebug 1h 墙钟**：超时作业 TIME OUT 会浪费——评估前预估时长，>1h 一律拆片（--start-index/--limit）或放 1a40
3. **监管吞吐**：排队衔接快，结果集中到达 → 核对优先于下一提交
4. **会话级限制**：编排随会话结束；每任务落盘即记录+推送保证接手
5. **token 开销**：监管 workflow 每任务 1-2 次、<15 agents；火力模式不用
6. **监控 cron**：已建（17,47，7 天过期），按作业登记板盯梢

---

## 八、近期执行计划 v2（2026-08-13 晚更新，取代前文 Phase 1-5 细节）

### 8.1 正在跑

| 作业 | 内容 | 预期出数 |
|---|---|---|
| 1670231 | Phase 3 第一仗:5 思路投票 × SFT 检查点 × 全量 1034 | ~01:30 |
| 1672524 | T2.3 训练最后一搏(finer + dr_grpo + lr 1e-6) | ~05:30 |

### 8.2 蒸馏备料三步走（待用户批准启动）

1. **50 题 A/B 实测**(CPU 分区,16 秒级):flash 思考 vs 非思考 各 25 题 + pro 抽样 5 题;检查:温度参数兼容、双 `<think>` 结构、max_tokens=4096 截断、真实单价
2. **配方定稿**:按实测定(预期 flash 非思考主力 + pro 补难题);数据来源=未分配池(与 GRPO 零交集, G3)
3. **5000 条放量**(CPU 分区并行,~1-2 小时,预算 ¥10-20):产出 data/reasoning_data/distill_v2_5000.jsonl → 下一轮 SFT(混合 gold 15%)→ 预期单次 60.8% → 65-70%

### 8.3 投票升级系列(与训练并行,优先级最高)

| 实验 | GPU 成本 | 预期 | 前置 |
|---|---|---|---|
| 投票甜点诊断 maj@{8,16,32,64}×temp{0.7,1.0} | 0.5-1 天 | +1~3pp + L7 论文核心数据 | 投票出数后 |
| memory 质心过滤消融 | 纯推理 0.5 天 | +1~2pp | 诊断曲线后 |
| few-shot 轨迹变体 | 0.5 天 | +1~2pp | 诊断曲线后 |
| MPEV 官方口径复测(管线 bug 已修) | 1 天 | 官方 +2~5pp | 随时 |

### 8.4 训练侧分支(明早 T2.3 出数后)

- **T2.3 ≥60.8%**:训练侧复活 → 大规模 GRPO(全量 8659 不相交 + G16-32 + 四分量,2-4 GPU 天,预期单次 68-76%)→ 投票 → 80%+ 同口径反超 FINER
- **T2.3 再败**:训练侧封盘(RL 对 3B 从强 SFT 起点不work 的三连证据, 本身是 L5 论文素材)→ 全力投票 + 蒸馏加料 → 保守线 70-75%

### 8.5 L7 机制论文数据(并行,大部分零成本)

- E0 回捞已完成(6/7 文件)→ E1 离线重分析(0 GPU)→ E2 诊断曲线(与 8.3 重叠)→ E3 全量 5p 补跑(1-1.5 GPU 天)→ E4 训练机制复测(1-2 GPU 天,可选)
- 时间线:随投票系列同步推进

### 8.6 里程碑时间线

| 时间 | 事件 |
|---|---|
| 今晚 01:30 | 投票出数(近期最确定的胜果, 预期 68-75%) |
| 明早 05:30 | T2.3 出数 → 训练侧终局判定 |
| 明后两天 | 蒸馏备料完成(若批准)+ 诊断曲线启动 |
| 1-2 周 | 投票升级全套 → 全量最优投票 71-78%;训练侧按分支推进 |
| 之后 | 论文数据齐备 → 进入 L1 技术报告写作(另行启动) |
7. **API 费用**：Phase 1 用已生成 1000 条，零新增；并行备料按 D5 批准执行

---

## 七、文献弹药库（2026-08-13 调研新增，详见 docs/research_track_20260813.md）

> 来源：文献研究轨道 workflow（12 篇调研 → 4 篇精读：FINER-SQL 原文 / Reasoning-SQL / PaVeRL / maj@ 饱和）。
> 定位：**备选实验弹药**，按优先级插队执行，不阻塞 Phase 1 提交主线。执行顺序建议：先零训练成本项（推理插队 gpudebug），训练项并入 Phase 2/5 消融。

### 7.1 零训练/纯推理项（优先，可随时插队）

| 实验 | 并入阶段 | 预期 | 成本 |
|---|---|---|---|
| 投票失败诊断曲线（maj@{8,16,32,64}×温度{0.7,1.0} + Pass@K/Average@K/Self-BLEU/执行分组） | Phase 3 | 定位本项目投票饱和点 +1~3pt；**L7 机制论文核心数据** | 纯推理 0.5-1 GPU 天 |
| Memory 质心推理端过滤消融（剔除相似度最低 20-30% 候选再投票） | Phase 3 | 票数集中 +1~2pt；机制论文受控数据点 | 纯推理 + CPU 嵌入 |
| 涌现轨迹 few-shot 投票变体（GRPO 成功轨迹做 few-shot，并行采样合并投票） | Phase 3 | +1~2pt（多样性来源） | 0.5 GPU 天 |
| OOD 体检（Spider-DK/Spider-Syn，SFT 后各测一次；需先下载数据集） | Phase 1 附加 | 防 SFT 记忆化塌方；决定"跳 SFT" | 0.5 GPU 天 |

### 7.2 训练项（并入 Phase 2 消融矩阵）

| 实验 | 预期 | 成本 |
|---|---|---|
| **QAE 分位数 baseline A/B**（组均值→K-分位数 K∈{0.3,0.4,0.5}，其余不动） | pass@1 +5~20%；熵稳定→投票多样性受保护 | 1-2 GPU 天 |
| **奖励栈 v2**（非负列级 R=10×EX_f + 列数惩罚防 SELECT * 投机 + "错<对"不变量回归校验） | +1~3pt 稳定性双升 | 2-4 GPU 天 |
| 熵崩溃监控 + 难度分桶（并入 grpo-preflight 检查清单） | 提前发现 3B 熵塌缩 | 0.5 人天 |
| 两阶段 GRPO 调度（PaVeRL 重启：10ep 余弦→best-ckpt 重启） | +0.5~1.5pt | 零新增 |

### 7.3 暂缓项（备查）

- judge 奖励（RLAIF）：仅 exec=0 时调强模型打分；文献 +1pt 但 API 预算敏感、需自行定标
- Verbal RL 蒸馏增强：API 成本极高，3B 自评质量不足可能倒贴

### 7.4 关键超参速查（文献实测）

FINER GRPO：lr=8e-6 / rollouts=32 / batch=32 / 2000 步 / 温度 1.0 / 候选 20-30 收敛 / SFT completion-only CE 2ep lr=2e-5。
Reasoning-SQL：lr=1e-6+0.1% warmup / batch=32 / G=6 / 3ep；**优势不除 std**；exec=3/judge=2/其余=1。
投票：maj@32 饱和；temp 1.0；30 候选甜点；GRPO 后需显式测 Pass@K vs Average@K（多样性下降）。
