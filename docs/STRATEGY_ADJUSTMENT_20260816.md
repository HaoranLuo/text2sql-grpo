# 实验策略调整（2026-08-16）

> 触发事件：sft_v3 出数 63.6%（+0.1pp，剂量饱和）+ ORM/5p×sft_v3 两个作业失败修复 + B2 三模型池跑中。

## 一、形势判断（三个事实）

1. **训练线"加量"已死**：教案 3.8×→+2.7pp、1.45×→+0.1pp，剂量响应三点点出对数饱和曲线。SFT v4 继续加料是浪费。
2. **剩余增量在两条线**：推理侧 ORM（训练型裁判，+2-5pp 文献实证）与训练侧"提质"（FINER 完整配方 或 难题定向轨迹合成）。
3. **B2 今天出数**：三模型池预期 75.5-76%（因 sft_v3 仅 +0.1pp，从 +2pp 下调到 +1~1.5pp）。

## 二、已执行的调整

1. **队列再插队**（ORM 提前 ~15h）：
   ```
   A40: 3b_1000st（用户，跑中）→ ORM 训练 → 5p×sft_v3 → E3#4
   ```
2. **FINER 完整配方脚本准备中**（agent 后台）：P1#4 规格（8659 不相交 + G16-32 + 四分量奖励 + DAPO 式梯度修复 + β 0.01-0.02 + 2000 步 + sft_v3 起点）——只准备不提交，等 3b_1000st 收敛数据出来做 GO/NO-GO。

## 三、训练侧策略转向：从"加量"到"提质"

| 路线 | 做法 | 预期 | 决策点 |
|---|---|---|---|
| A. FINER 完整配方 | 全量数据 + 四分量 + 梯度修复 GRPO | 单次 68-76%？→ 叠 MI-VAV 78-80% | 等 3b_1000st 出数（收敛曲线证据）|
| B. 难题定向轨迹合成 | DeepSeek API 合成"分解+修订"多步轨迹（5% 无解题 + hard/extra），RevDecomp-SFT 式 | 难题 +3~6 | B2 出数后按无解题清单执行（API ~¥10-20）|
| C. 7B 基座 | 全套管线换 7B | 80-82% | 论文扩展章节，最后做 |

## 四、推理侧路线（优先级最高）

1. B2 三模型池 → 今天出数 → 若 ≥75.5% 定稿"MI-VAV + 三 checkpoint 池"为 L4 方法
2. ORM 训练（队列前排）→ ORM 集成实验（平票裁决器 + 池内 rerank）→ 目标 76-78%
3. 合流实验：三模型池 × ORM 终裁（B2 与 ORM 的组合）

## 五、纪律修正

- **链式作业显式声明依赖**（slurm --dependency）：不再依赖提交顺序（本次 5p×sft_v3 顺序异常的教训）
- 队列操作后立即 verify squeue 顺序

## 六、论文线（待用户点头，0 GPU）

- L7 骨架（选择缺口曲线 + 验收阶梯 + 口径错位四证 + 剂量饱和曲线——数据全齐）
- L4 方法章节草稿（MI-VAV + 消融矩阵 + 对比表，新颖性检索已定差异化三点）
