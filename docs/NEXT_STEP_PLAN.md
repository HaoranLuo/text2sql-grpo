# 下一步实验计划（2026-08-18）+ 加速方案

> 目标：74.3% → 78-80% 官方。三层贡献链：MI-VAV（已得）+ ORM 裁判 + 训练配方。

## 一、实验序列（按依赖排序）

### 第 1 步：ORM 四臂对决（今天，~2h 后自动开始）
- ORM 训练中（重投 1996104，第三次 bug 已修，本次训练+评估+保存全链路预期跑通）
- 出模型后立即跑 3090 两段式四臂实验（脚本就绪：argmax/grouphead/tiebreak/hybrid）
- **判据**：任一臂官方 ≥ 74.3% + 1pp → ORM 集成进主流程；否则记录负结果

### 第 2 步：SFT v4-hard（难题补课训练，~4h，★建议先于 FINER）
- 308 条难题轨迹（讲解式 273 + 自由生成 35）混入 v2 配方 5-7%
- **为什么先于 FINER**：① 便宜（4h vs 44h）② 低风险（确定性轨迹）③ 若单次 +0.5-1.5pp，FINER 可以从 v4-hard 这个**更强的起点**出发——顺序对 FINER 本身也有利
- 判据：单次官方 ≥ 64.5% → 值得；叠 MI-VAV 目标 75%+

### 第 3 步：FINER 完整配方（44h，待你批准）
- 从（第 2 步成功的）v4-hard 起点出发；四分量降三份量（memory 缺失）+ 去 std + dr_grpo + G16 + 5500 条
- 判据：单次官方 ≥ 66% → 叠 MI-VAV 冲 76-78%

### 第 4 步：合流（全部成功后）
- 最强 SFT 池（v1/v2/v4-hard/FINER 各 16 候选）+ MI-VAV + ORM 终裁 → 目标 78-80%

## 二、加速方案（已部署 + 新增）

| 加速项 | 状态 |
|---|---|
| ORM 四臂走 3090 + 官方复评走 CPU（不占 A40） | ✅ 脚本就绪 |
| 难题轨迹合成（API，零 GPU） | ✅ 已完成 308 条 |
| SFT v4-hard 数据准备（现在派 agent，训练前备好） | 🔧 立即启动 |
| SFT v4-hard 先于 FINER（快收益 + 给 FINER 更强起点） | 📋 建议 |
| FINER 44h 直接开跑（A40 队列空，零排队） | 📋 待批准 |
| L7 论文骨架写作（0 GPU 并行） | 📋 待点头 |

## 三、时间线（若全部批准）

```
今天    ORM 出模型 → 四臂对决出数（~4h 后）
明天    SFT v4-hard 训练（4h）→ 单次+投票出数
后天    FINER 开跑（44h，若 SFT v4-hard 成功则从它起步）
+2 天   FINER 出数 → 单次+MI-VAV 出数
+3 天   合流实验 → 最终数字（78-80% 或诚实上限报告）
```

## 四、需要你批准的两件事

1. **SFT v4-hard 训练**（~4h A40，低风险，预期 +0.5-1.5pp）
2. **FINER 完整配方**（44h A40，胜率 ~50%，预期 +1-3pp；失败也有论文价值）

两者我都推荐 GO，且建议按 2→3 的顺序执行。
