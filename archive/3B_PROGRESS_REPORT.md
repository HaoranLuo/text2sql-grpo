# 进度报告：面向 Text-to-SQL 的 3B 推理生成器 GRPO 微调

> **日期**：2026-08-03
> **作者**：jiahuiwang24
> **平台**：西交利物浦大学（XJTLU）高性能计算平台
> **范围**：第一阶段实验，在扩展到更大规模之前，先在小模型上验证基于强化学习的 Text-to-SQL 方案

---

## 1. 项目概述

### 1.1 研究目标

本项目研究强化学习（GRPO，组相对策略优化）能否提升小型开源模型作为"推理生成智能体"（Reasoning Generator Agent）的 Text-to-SQL 性能。该智能体先逐步生成推理轨迹，再输出最终 SQL 查询。

项目的动机来自 OmniSQL-32B 研究方向：大模型训练和迭代的成本高昂。本工作先在 3B 模型上把完整流程跑通，包括数据集准备、智能体构建、GRPO 训练和评估，之后再将方案稳妥迁移到更大的模型（7B 到 32B）。

### 1.2 核心问题

> 在单张消费级 GPU 和极小训练预算下，对 Qwen2.5-Coder-3B-Instruct 基座模型做 GRPO + LoRA 微调，能否提升其在 Spider Text-to-SQL 基准上的执行准确率？

### 1.3 总体方法

```
Spider 训练集          Spider 开发集
（前 100 条）          （前 100 条）
        │                        ▲
        ▼                        │
  GRPO + LoRA 训练 ──► 评估（训练前 vs 训练后）
  （40 步、2 个候选）           同一批 100 条样本
        │
        ▼
  对比：基线 37% vs GRPO 后 39%（执行匹配率）
        基线 59% vs GRPO 后 64%（执行成功率）
```

评估采用"训练前后对照"协议：同一批 100 条 Spider 开发集样本在训练前（基线）和训练后（GRPO）分别输入模型，逐项比较各指标。生成过程中模型看不到金标准 SQL，金标准 SQL 只用于奖励计算。

---

## 2. 为什么选择 3B 模型

| 标准 | 3B 模型的选择 | 收益 |
|---|---|---|
| **硬件** | 配合 LoRA 和梯度检查点可放入单张 RTX 3090（24 GB） | 不依赖多卡 |
| **迭代速度** | 约 40 训练步，约 1 小时（gpudebug 分区） | 实验循环快 |
| **概念验证** | 端到端验证完整流程（数据 → GRPO → 评估） | 为扩展到 7B/32B 降低风险 |
| **成本** | 训练预算：100 条样本、2 个候选 | 几乎为零（免费 HPC 配额） |
| **诊断价值** | 小模型能更快暴露奖励稀疏和过拟合问题 | 修正信号更清晰 |

3B 模型是验证载体，不是最终产品。项目背景中的明确目标是："用较小语言模型替代原有大模型，验证用 GRPO 训练推理生成智能体能否提升 Text-to-SQL 性能。"

---

## 3. 技术方案

### 3.1 环境

| 组件 | 取值 |
|---|---|
| HPC 平台 | XJTLU HPC（login.hpc.xjtlu.edu.cn），SLURM |
| GPU | 3B 实验：NVIDIA RTX 3090（24 GB）；较大规模实验：NVIDIA A40（48 GB） |
| Python | 3.10.20（conda 环境 reasoning3b） |
| PyTorch | 2.5.1+cu124 |
| Transformers | 4.48.3 |
| TRL | 0.15.2（GRPOTrainer、GRPOConfig） |
| PEFT | 0.14.0 |
| Accelerate / Datasets | 1.3.0 / 3.2.0 |

### 3.2 模型

- 基座模型：Qwen2.5-Coder-3B-Instruct（3B 参数，面向代码和推理优化），以 bf16 本地加载。
- 训练方法：GRPO + LoRA，仅约 0.3% 的参数可训练（3B 参数中约 1000 万）。

### 3.3 数据集

- Spider（data/spider_data/）：train_spider.json（训练）、dev.json（评估）、tables.json（表结构元数据）。
- 训练集：train_spider.json 前 100 条（激进实验扩大到 1000 条）。
- 评估集：dev.json 前 100 条，所有实验完全相同，保证可比。
- 每条样本包含：prompt（用智能体自身的 11 条指令提示词构建，保证训练与推理一致）、question、query（金标准 SQL，仅用于奖励）、db_id、ddl（从 sqlite_master 提取，缺失时回退到 tables.json）。

### 3.4 GRPO 训练配置（标准实验）

| 参数 | 取值 | 理由 |
|---|---|---|
| 训练样本 | 100 | 小预算验证 |
| 训练步数 | 40 | 受 gpudebug 分区 1 小时墙钟时间限制 |
| 每组生成数（组大小） | 2 | 适配 24 GB 显存 |
| 学习率 | 5e-6 | LoRA 微调的标准值 |
| KL 惩罚系数 β | 0.04 | 让策略贴近参考模型 |
| 采样温度 | 0.7 | 保证候选多样性 |
| 最大提示长度 | 1536 tokens | |
| 最大补全长度 | 256 tokens | SQL 和推理的预算 |
| 梯度检查点 | 开启 | 节省显存 |
| 精度 | bf16 | |

### 3.5 LoRA 配置

| 参数 | 取值 |
|---|---|
| 秩 r | 16 |
| Alpha | 32 |
| Dropout | 0.05 |
| 目标模块 | q_proj、k_proj、v_proj、o_proj、gate_proj、up_proj、down_proj（全部 7 个） |
| 可训练参数 | 约 0.3%（3B 中约 1000 万） |

### 3.6 奖励函数（第三阶段设计，后续演进）

```
生成 SQL
    ↓
在 SQLite 上执行（只读，带安全检查）
    ↓
结果行与金标准结果行一致 → 1.0
否则                      → 0.0   （二值版本）
```

- 排序感知：若金标准 SQL 含 ORDER BY，比较时保留行顺序。
- 后续演进为三级奖励（见 4.3 节）。

### 3.7 评估指标

| 指标 | 定义 |
|---|---|
| **解析成功率** | 能从预测中提取出 SQL 语句的比例（正则代码块解析器） |
| **执行成功率** | 预测 SQL 能在 SQLite 上无错误执行的比例 |
| **执行匹配率** | 预测 SQL 的结果行与金标准 SQL 结果行完全一致的比例（主指标） |
| SQL 字符串匹配率 | 归一化后的字符串相等（1%，接近下限，因为结果格式多样） |

说明：主指标是自定义执行匹配，不是官方 Spider 评估器。官方 test-suite-sql-eval 集成已列入计划（见第 9 节）。

---

## 4. 实验设计与依据

### 4.1 为什么用 GRPO 而非 PPO

| | PPO | GRPO |
|---|---|---|
| 所需模型 | 策略 + 价值（critic）+ 参考 + 奖励 | 仅策略 + 参考 |
| Critic 模型 | 必需（价值函数） | **不需要**，改用组内相对基线 |
| 内存占用 | 约两倍（critic 让参数翻倍） | 约一半 |
| 小模型、单卡适配 | 差 | **好** |

GRPO（DeepSeek, 2024）在组内计算优势：模型对每个 prompt 生成 N 个候选 SQL，逐个由奖励函数打分，高于组均值的候选被强化，低于组均值的被推开。这去掉了 PPO 中内存开销最大的价值模型（critic），在 24 GB GPU 上是决定性优势。

### 4.2 为什么用 LoRA

- 仅约 0.3% 的参数可训练（7 个注意力/前馈投影上 r=16），配合梯度检查点，24 GB 内放得下 3B 模型和优化器状态。
- 检查点体积小（几十 MB 的适配器），可以在 25、50、75 步做廉价的检查点评估。
- 迭代快：100 条样本上 40 步 GRPO 在一个小时调试分区时段内就能完成。

### 4.3 为什么从二值奖励演进到三级奖励

- 第三阶段规格：二值奖励（正确 1.0，错误 0.0），简单且可解释。
- 观察：二值奖励下 GRPO 过拟合。1000 条实验退化到基线以下（33% vs 37%），因为奖励无法区分"可执行但结果错误"（通向正确的良好一步）和"语法错误"（死胡同），信号太稀疏，引导不了策略。
- 文献证据（Agentar-Scale-SQL，蚂蚁集团，BIRD 第一名 81.67%）：三级奖励是杠杆最高的修正：

```
1.0   结果行与金标准一致      （完全正确）
0.1   SQL 可执行但结果错误    （部分得分，缺失的信号）
0.0   语法错误或不可执行
```

这一行改动（train_reasoning_grpo.py 中用 --reward-type three_level 实现）把稀疏的二值奖励变成分级奖励，让"可执行但错误"的候选也能给策略提供学习信号，不再和垃圾输出等同对待。

### 4.4 为什么尝试 SFT 冷启动

在 RL 之前，策略在温度 0.7 下生成的补全质量低，稀疏奖励下（N 个候选中很少正确）从零开始 GRPO 效率低，而且容易奖励黑客。

SFT 冷启动的做法：用更强的教师模型（DeepSeek V4 Flash API，温度 0.3，max_tokens 1024）为 500 条 Spider 训练样本生成推理和 SQL（src/generate_api_data.py），再在这些轨迹上对 7B 模型做 SFT（LoRA r=16，2 个 epoch，lr 2e-5，src/train_sft.py），在 GRPO 之前模仿专家推理分布。

理由：SFT 先把策略预热，让 GRPO 从正确 SQL 更常见的分布出发，组相对优势信号更密，KL 惩罚的破坏也更小。这与推理 LLM 文献中"先模仿后 RL"的配方一致（DeepSeek-R1 流程、CSC-SQL、FINER-SQL）。

---

## 5. 全部 3B 实验

所有实验共用：基座模型 Qwen2.5-Coder-3B-Instruct，LoRA r=16/alpha=32/dropout=0.05，温度 0.7，lr 5e-6，β=0.04（另有注明除外），评估用同一批 100 条 Spider 开发集样本。

### 5.1 实验总表

| # | 实验 | 训练数据 | 步数 | 候选数 | 奖励 | 评估：解析 | 评估：执行成功 | 评估：**匹配** | 对比基线 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **零样本基线** | — | — | — | — | 98/100（98%） | 59/100（59%） | **37/100（37%）** | — |
| 2 | GRPO（初版） | 100 | 40 | 2 | 二值（0/1） | 98/100（98%） | 64/100（64%） | **39/100（39%）** | **+2.0%** |
| 3 | GRPO（激进），ckpt 25 | 1000 | 25 | 4 | 二值（0/1） | — | — | — | — |
| 3 | GRPO（激进），ckpt 50 | 1000 | 50 | 4 | 二值（0/1） | — | — | — | — |
| 3 | GRPO（激进），ckpt 75 | 1000 | 75 | 4 | 二值（0/1） | — | — | — | — |
| 3 | GRPO（激进），ckpt 100 | 1000 | 100 | 4 | 二值（0/1） | — | — | **33/100（33%）** | **−4.0%（过拟合）** |

*实验 3 的各配置通过 scripts/eval_checkpoints.slurm 在 25/50/75/100 检查点评估，中间检查点指标记录在 HPC 日志中。激进实验配置为最多 200 步（scripts/train_3b_aggressive.slurm）、1000 条样本、4 个生成；按研究计划记录的实验实际为 100 步。*

### 5.2 运行详情

**实验 1，零样本基线**（outputs/baseline_pretrain_100/）
- 命令：python src/run_spider_baseline.py --limit 100 --model-path models/Qwen2.5-Coder-3B-Instruct
- 结果：解析 98/100，执行 59/100，**匹配 37/100**，字符串匹配 1/100，平均生成 12.6 秒/样本，总计 1265 秒。

**实验 2，GRPO 40 步**（checkpoints/grpo_lora/）
- 命令：python src/train_reasoning_grpo.py --num-train 100 --num-generations 2 --max-steps 40 --learning-rate 5e-6 --beta 0.04 --temperature 0.7
- 训练动态：平均奖励在 0.1 到 0.4 之间波动（起点 0.30）；奖励标准差 0.42 → 0.14；KL 0.0014 → 0.0004；梯度范数 27.1 → 0.013；补全长度约 256（达到上限）。
- 训练后评估（outputs/grpo_post_train_100/）：解析 98/100，执行 **64/100**，匹配 **39/100**；平均生成 3.6 秒/样本（批处理），总计 355 秒。

**实验 3，GRPO 激进（1000 条）**（checkpoints/grpo_3b_aggressive/）
- 命令：--num-train 1000 --num-generations 4 --max-steps 200 --learning-rate 5e-6 --beta 0.04
- 每 25 步保存一次检查点，每个检查点在同一个 100 条开发集上评估。
- 最终（ckpt-100）：**33%**，低于 37% 的零样本基线，明显过拟合（见 6.2 节）。

---

## 6. 关键发现（3B 阶段）

### 6.1 GRPO 确实提升了 3B 模型，但幅度有限

| 指标 | 基线 | GRPO 后（40 步） | 变化 |
|---|---|---|---|
| 执行匹配 | 37% | **39%** | **+2.0%** |
| 执行成功 | 59% | **64%** | **+5.0%** |
| 解析成功 | 98% | 98% | 0 |
| 字符串匹配 | 1% | 1% | 0 |

绝对值：多答对 2 题（37 到 39/100），多 5 条 SQL 可执行（59 到 64）。执行成功率 +5% 是典型的 RL 早期特征：模型先学会不生成坏 SQL（别名修正如 s.score_id → sc.score_id、JOIN 条件修正、列名拼写），再学会生成正确的 SQL。40 步完成了第一阶段。

### 6.2 扩大规模后出现过拟合

把实验扩大到 **1000 条 × 4 个候选 × ≥100 步**，结果适得其反：

| 3B 实验 | 执行匹配 |
|---|---|
| 零样本基线 | 37% |
| GRPO 40 步（100 条、2 生成） | **39%** |
| GRPO ckpt-100（1000 条、4 生成） | **33%** |

更大规模的实验最终低于基线 4 个点。策略记住了 1000 条训练样本，泛化更差。100 条实验让策略保持在参考分布附近（KL 低），1000 条加二值奖励的实验则漂移进了过拟合区域。**二值奖励下，更多数据加更多步数对 3B 没有帮助。**

### 6.3 最佳区间：100 条样本上 40 到 75 步

跨实验证据：
- 3B：40 步 +2%，100 步以上过拟合。
- 7B：二值奖励 25 步即降到 79%（−2%），三级奖励 25、50 步为 77%、79%，短训练也没有超过基线（详见第 7 节）。

可靠运行窗口是 100 条子集上 40 到 75 步、小组大小（2 个生成）：足以拿到语法质量收益，又不至于陷入奖励优化的过拟合。不过 7B 的结果表明，即使在这个"短训练"区间内，强基座模型上的 RL 也没有超过零样本基线（见 7.4 节），数据量和奖励信号密度才是更紧的约束。

### 6.4 更多数据（1000 条）对 3B 没有帮助

二值奖励下，1000 条实验严格更差（33% vs 39%）。解释：0/1 奖励下策略无法区分"可执行但错误"和"垃圾输出"，只能被推向组内均值局部所奖励的方向，额外数据放大的是奖励黑客和记忆，不是泛化。这推动了奖励重设计，而不是加数据。

### 6.5 二值奖励不足，这是核心诊断

所有用二值奖励的实验要么过拟合，要么停滞。1000 条实验的崩塌和 7B 的 −2% 退化都归因于奖励稀疏。与文献一致的修正（Agentar-Scale-SQL）是三级奖励，给"可执行但错误"的 SQL 一个 0.1 的部分得分层，已实现（--reward-type three_level），并成为当前和后续实验的主干（第 7、9 节）。

### 6.6 小规模训练是稳定的

40 步实验中：KL 0.0014 → 0.0004（无漂移），奖励标准差 0.42 → 0.14（候选质量收敛），梯度范数 27.1 → 0.013（收敛）。因此过拟合不是训练不稳定，而是奖励设计和容量的问题。

---

## 7. 向 7B 演进

### 7.1 为什么更换模型

3B 实验确立了当前方案的天花板在约 39% 到 40% 的执行匹配率，更重要的是定位了瓶颈：奖励稀疏和模型容量，不是 RL 机制本身。Qwen2.5-Coder-7B-Instruct 零样本基线达到 **81%**，是 3B 训练后结果的 2 倍以上，证实模型规模压倒了在 3B 上尝试的各种技巧。

### 7.2 7B 实验总结（结果已全部回收）

| # | 实验 | 训练数据 | 步数 | 候选数 | 奖励 | 结果（匹配率） |
|---|---|---|---|---|---|---|
| 1 | **7B 零样本基线** | — | — | — | — | **81%**（解析 100%、执行 97%） |
| 2 | 7B GRPO（二值奖励） | 100 | 25 | 2 | 二值（0/1） | **79%（−2%）** |
| 3 | 7B GRPO（三级奖励） | 100 | 25 | 2 | 三级（1.0/0.1/0.0） | **77%（−4%）** |
| 4 | 7B GRPO（三级奖励） | 100 | 50 | 2 | 三级（1.0/0.1/0.0） | **79%（−2%）** |
| 5 | 7B GRPO 调参（lr=1e-6、β=0.1） | 100 | 25 | 2 | 三级 | **79%（−2%）** |
| 6 | 7B + SFT（DeepSeek API 500 条，2 epochs） | 500（API） | SFT 2 epochs | — | 交叉熵 | **71%（−10%）** |
| 7 | 7B + SFT + GRPO | 500 + 100 | SFT 2 epochs；GRPO 25 | 2 | 三级 | **78%（+7% vs SFT）** |
| 8 | 7B + SFT + GRPO | 500 + 100 | SFT 2 epochs；GRPO 50 | 2 | 三级 | **78%（+7% vs SFT）** |
| 9 | CSC-SQL-7B（零样本参考） | — | — | — | — | **73%（−8%）** |
| 10 | XiYanSQL-7B（零样本参考） | — | — | — | — | **44%（−37%，格式不匹配）** |
| 11 | OmniSQL-7B（零样本参考） | — | — | — | — | **66%（−15%）** |

所有 7B 实验均已在 HPC（A40）上完成，结果已回收。**没有任何微调超过 7B 零样本基线（81%）：所有 RL/SFT 实验要么退化，要么只恢复到基线附近。**

### 7.3 7B 阶段的设计逻辑

1. 确立 7B 天花板（零样本 81%），已完成。
2. 验证二值奖励 GRPO 在 7B 上也过拟合（25 步 79%），已完成，证实诊断与模型规模无关。
3. A/B 测试奖励修正：受控条件下三级 vs 二值（25 步 vs 50 步），exp_reward 实验，已完成。
4. 抑制优化漂移：降低学习率（1e-6）、增强 KL 惩罚（β 0.1），grpo_tuned 实验，已完成。
5. 用 SFT 冷启动：GRPO 前模仿 DeepSeek V4 Flash 推理轨迹，sft_grpo_pipeline 实验，已完成。
6. 与已发表模型对比：CSC-SQL、XiYanSQL、OmniSQL 零样本作为外部参考点，已完成。

研究计划曾预期"7B + 三级奖励 + 40 到 50 步 → 81% → 85-88%"。实际结果没有达到预期（最高 79%），原因分析见 7.4 和 7.5 节。

### 7.4 7B 逐实验详细结果

**E4，7B 零样本基线**（约 2026-08-02，A40）
- 解析 **100%**，执行 **97%**，**匹配 81%**。
- 规模主导：7B 零样本（81%）比 3B 零样本（37%）高 44 个点，超过此前测得的任何微调增益。7B 生成近乎合法的 SQL（97% 执行率），剩余差距在语义层（结果正确性），不在语法层。至今仍是本项目最佳模型。

**E5，7B + GRPO 二值奖励 25 步**（约 2026-08-02，A40）
- 配置：100 条样本、2 个候选/组、二值奖励、lr 5e-6、β 0.04、温度 0.7。
- 匹配 **79%**（较基线 −2%）。
- GRPO 伤害了强基座模型。和 E3 相同的二值奖励过拟合，这次发生在本身已经很好的模型上：100 条样本上的 RL 把策略推离其强先验，却没有带来正确性的净增益。

**E6，7B + GRPO 三级奖励 25 步**（约 2026-08-02，A40）
- 配置：100 条样本、2 个候选/组、三级奖励（1.0/0.1/0.0）、lr 5e-6、β 0.04。
- 匹配 **77%**（−4%）。
- 三级奖励（Agentar-Scale-SQL 的 P0 方案）没有在 100 条样本上拯救 7B。训练不足导致矫正不足，数据极小时密集信号本身不足以带来提升。

**E7，7B + GRPO 三级奖励 50 步**（约 2026-08-02，A40）
- 配置：100 条样本、2 个候选/组、三级奖励、lr 5e-6、β 0.04。
- 匹配 **79%**（−2%）。
- 50 步好于 25 步（79% vs 77%），但仍然低于基线。同级步数下三级奖励只比二值奖励有微幅恢复（E5 二值同为 79%），从未超过 81%。

**E8，7B + GRPO 调参（lr=1e-6、β=0.1）**（约 2026-08-02 至 08-03，A40）
- 配置：25 步、100 条样本、2 个候选/组、三级奖励、学习率 1e-6（降为 1/5）、β 0.1（提高 2.5 倍）、温度 0.7。
- 匹配 **79%**（−2%）。
- 更强的 KL 正则加更小的学习率在 100 条样本上没有帮助，和未调参的 50 步同为 79%。瓶颈是数据量和奖励信号密度，不是优化不稳定性。

**E10，7B + SFT 冷启动（DeepSeek API 500 条）**（约 2026-08-03，A40）
- 配置：500 条 API 生成样本（data/api_sft_data.json）、2 epochs、lr 2e-5、batch 1、梯度累积 4、最大长度 2048、bf16、LoRA r16。
- 匹配 **71%**（−10%）。
- 对 API 生成数据的 SFT 主动伤害性能：模仿 DeepSeek 的推理/SQL 格式把策略推离 7B 自身的强项，500 条混合质量的 API 样本劣于零样本。单独 SFT 不是可行的冷启动。

**E11/E12，7B + SFT + GRPO（25 步 / 50 步）**（约 2026-08-03，A40）
- 配置：在 SFT 模型上做 GRPO，100 条样本、2 个候选/组、三级奖励、lr 5e-6、β 0.04。
- 匹配 **78%**（较 SFT 的 71% 恢复 +7%）。
- RL 作为修复机制是有效的：从 SFT 的 71% 恢复到 78%，接近零样本的 81% 但仍有差距。25 步和 50 步结果相同（78%），出现平台期。SFT+GRPO 流水线不优于直接零样本，在此数据规模下流水线的复杂度没有换来收益。

**E9，CSC-SQL-7B（第三方 GRPO 预训练，零样本）**（约 2026-08-03，A40）
- 模型：cycloneboy/CscSQL-Grpo-Qwen2.5-Coder-7B-Instruct，只评估不训练。
- 匹配 **73%**（−8%）。
- 在我们自己的提示词和评估条件下，第三方 GRPO 调优的 7B 仍输给原版 7B 零样本（73% vs 81%）。要么是它的训练分布不同，要么是我们的提示词模板没有触发它的调优行为。在自定义评测框架下，外部检查点不是越过基线的捷径。

**E13，XiYanSQL-7B（零样本）**（约 2026-08-03，A40）
- 模型：XGenerationLab/XiYanSQL-QwenCoder-7B-2502，只评估。
- 匹配 **44%**（−37%）。
- 格式不匹配：XiYanSQL 用自带的 schema 链接和输出格式训练，输出额外标记而不是我们的 ```sql 代码块，我们的解析器在大多数输出上失败。44% 主要反映解析失败，不代表模型质量。公平对比前需要做提示词和格式适配。

**E14，OmniSQL-7B（零样本）**（约 2026-08-03，A40）
- 模型：seeklhy/OmniSQL-7B，只评估。
- 匹配 **66%**（−15%）。
- 在我们的评测框架下远好于 XiYanSQL（66% vs 44%），但仍然低于原版 Qwen 7B 零样本（81%）。与 E9 结论相同：外部 Text-to-SQL 检查点在我们的流水线里输给基座模型。

**7B 实验超参数速查**

| 实验 | 模型 | 步数 | 训练数据 | 生成/组 | 奖励 | lr | β | 温度 | GPU |
|---|---|---|---|---|---|---|---|---|---|
| E5 | 7B | 25 | 100 | 2 | 二值 | 5e-6 | 0.04 | 0.7 | A40 |
| E6 | 7B | 25 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |
| E7 | 7B | 50 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |
| E8 | 7B | 25 | 100 | 2 | 三级 | **1e-6** | **0.1** | 0.7 | A40 |
| E10（SFT） | 7B | 2 epochs | 500（API） | — | 交叉熵 | 2e-5 | — | — | A40 |
| E11/E12 | 7B+SFT | 25 / 50 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |

通用配置：LoRA r=16/alpha=32/dropout=0.05、7 个模块；TRL 0.15.2 GRPOTrainer；最大提示 1536；最大生成 256；梯度检查点。

### 7.5 7B 阶段结论

1. 100 条样本上，任何 RL 变体（二值/三级、25 到 100 步、调参）都没有超过零样本基线：7B 的 GRPO 结果全部落在 77% 到 79%，全部低于 81%。
2. 奖励密化（三级）消除了二值奖励式的崩塌过拟合，但没有转化为超过基线的收益。数据量这么小的时候，密集信号本身不足以驱动超越。
3. SFT 冷启动是净损失（−10%），RL 可以修复大部分损失（+7% 恢复到 78%），"先模仿后 RL"流水线在此数据规模下收益有限。
4. 7B 的执行率 97% 对匹配率 81% 之间 16 个点的差距是语义错误（错表、错列、错 JOIN），这正是 RL 应该解决的"硬推理"部分，但 100 条样本的奖励信号不足以教会它。
5. 外部模型（CSC-SQL 73%、OmniSQL 66%、XiYanSQL 44%）在统一评测下全部输给原版 Qwen2.5-Coder-7B 零样本（81%）。
6. 训练动态始终健康（KL 下降、奖励标准差下降、梯度范数趋近 0），即使指标在降。优化器没有问题，奖励信号和数据才是问题。

---

## 8. 经验教训

### 8.1 模型规模远大于训练技巧

零样本 7B（81%）击败完整 GRPO 训练过的 3B（39%），超过 2 倍。在 Text-to-SQL 上，基座容量压倒奖励工程、组大小和步数。3B 的训练时间对流水线验证是值得的，但容量是硬约束。7B 实验是对的方向，后续应该转向数据规模和奖励精化（见第 9 节）。

### 8.2 二值奖励导致过拟合

3B（1000 条，33%）和 7B（二值 25 步，79%）的二值奖励实验都退化到基线以下。0/1 奖励没有梯度区分"可执行但错误"和"不可解析"，策略只能过优化组内相对噪声。三级奖励（1.0/0.1/0.0）是单行代码里杠杆最高的修正，已成为默认（reward-type=three_level）。但 100 条样本下它只止住了崩塌，没有超过基线，需要在更大的数据上验证。

### 8.3 SFT 冷启动需要比原始 API 轨迹更高质量的数据

SFT 设计（500 条 DeepSeek API 轨迹、2 epochs）遵循"先模仿后 RL"的配方，但原始 API 补全没有针对金标准 SQL 做正确性过滤。实验证实这类数据会造成 −10 点的伤害（E10），RL 随后能修复其中大部分（+7 点，E11/E12）。今后应在模仿训练前用执行匹配过滤 SFT 语料，混合正确和错误的轨迹会注入噪声，KL 惩罚只能与它对抗。

### 8.4 Spider 评估方法

- 字符串匹配在这里没有用：只有 1/100（1%）。结果相同的不同 SQL 写法（比如 age > 20 和 20 < age）永远不会字符串匹配。执行匹配才是正确的主指标。
- 自定义 custom_execution_match（行集相等、ORDER BY 感知）是快速的训练前基线，明确不是官方 Spider 指标。官方数字需要 test-suite-sql-eval 评估器（全量开发集上的精确匹配加执行准确率），列入最终报告计划。
- 评估协议的质量要点：所有实验共用同一份 100 条开发集切片；金标准 SQL 从不展示给模型；SQLite 只读执行并带安全检查。

### 8.5 工程经验（HPC / TRL）

- TRL 0.15.2 改了 API 名：reward_funcs（不是 reward_functions）、processing_class（不是 tokenizer）。
- per_device_train_batch_size 必须是 num_generations 的整数倍。
- 24 GB 显存不足时：梯度检查点 + 截断序列长度 + 2 个候选。
- 右填充下批推理解析率掉到 40%，改用左填充。
- 访问 GPU 节点需要先 salloc 再 srun --pty bash；gpudebug 分区上限 1 小时（40 步的原因）；正确的 QoS 是 1a40（查自 sacctmgr）。
- JSON 里的 set 对象要转成 sorted(list(...))；进度条写到 stderr（查 .err 文件）。

---

## 9. 当前状态与下一步

### 9.1 状态（2026-08-03）

- 已完成：3B 流水线完整验证，基线 37% → GRPO 39%，执行 59% → 64%。
- 已完成：过拟合的定位和诊断（二值奖励 + 规模）。
- 已完成：7B 基线确立，零样本 **81%**，当前已知最佳结果，没有任何微调超过它。
- 已完成：奖励重设计（three_level）实现；SFT 冷启动流水线构建并执行。
- 已完成：7B 的 A/B 奖励实验、调参 GRPO、SFT+GRPO、外部模型评估全部在 HPC（A40）上执行完毕，结果已汇总（见 7.2、7.4 节）。最高 79%，没有微调超过 81% 基线。
- 已完成：全部 14 个实验（E1 到 E14），当前没有运行中的实验。

### 9.2 按优先级排序的下一步

| 优先级 | 行动 | 预期收益 | 工作量 |
|---|---|---|---|
| **P0** | 三级奖励在更大数据上重试：训练样本从 100 条扩到 ≥500 到 1000 条。100 条下三级奖励没有超过基线（77% 到 79%），数据量是疑似瓶颈 | 验证奖励信号密度假设 | A40 上 2 到 6 小时 |
| **P0** | 集成官方 Spider 评估器（test-suite-sql-eval，精确匹配 + 执行准确率），在全量开发集上验证 | 论文级指标 | 中 |
| P1 | 列级部分分（PaVeRL-SQL 风格：matched_cols / gold_cols） | +3 到 5% | 低 |
| P1 | 参考 FINER-SQL 实现（3B + GRPO + 记忆奖励 → 85% Spider，单 GPU 可复现），引入原子操作奖励 | +3 到 8% | 中 |
| P1 | 用执行匹配过滤 SFT 语料后再做模仿训练 | 提升 SFT 质量 | 低 |
| P1 | 为 XiYanSQL-7B 和 OmniSQL-7B 适配提示词/格式，做公平对比 | 公平对比 | 低 |
| P2 | AutoLink 式 schema 链接（bge-large-en-v1.5 列检索） | +3 到 8% | 中 |
| P2 | 多候选生成 + 执行投票（4 到 8 个候选，锦标赛） | +3 到 5% | 中 |
| P2 | 更大的数据切片（500 → 7000 训练行）后再给 RL 下结论 | 验证数据量假设 | 中 |
| P3 | 过程奖励模型（RuCo-C 式 rubric 批评）；agentic GRPO（AGRO-SQL） | +8 到 12% | 高 |

### 9.3 预期轨迹

```
3B 零样本          37%
3B GRPO（40 步）    39%   ← 3B 天花板已定位
7B 零样本          81%   ← 容量跃升，当前最佳
7B + 三级奖励      79%   ← 100 条样本下未超基线，数据量是疑点
7B + 更大数据 RL   85%+   ← 待验证（≥500 到 1000 条 + 三级奖励/列级部分分）
32B/OmniSQL 路径   ~90%   ← 长期
```

---

## 10. 关键指标汇总表

| 模型 | 方法 | 训练集 | 步数 | 候选数 | 奖励 | 解析 | 执行成功 | **执行匹配** | 对比自身基线 |
|---|---|---|---|---|---|---|---|---|---|
| 3B | 零样本基线 | — | — | — | — | 98% | 59% | **37%** | — |
| 3B | GRPO + LoRA | 100 | 40 | 2 | 二值 | 98% | 64% | **39%** | **+2.0%** |
| 3B | GRPO + LoRA（激进） | 1000 | 100（ckpt） | 4 | 二值 | — | — | **33%** | **−4.0%（过拟合）** |
| 7B | 零样本基线 | — | — | — | — | 100% | 97% | **81%** | — |
| 7B | GRPO + LoRA（二值） | 100 | 25 | 2 | 二值 | — | — | **79%** | **−2.0%** |
| 7B | GRPO + LoRA（三级） | 100 | 25 | 2 | 三级 | — | — | **77%** | **−4.0%** |
| 7B | GRPO + LoRA（三级） | 100 | 50 | 2 | 三级 | — | — | **79%** | **−2.0%** |
| 7B | GRPO 调参（三级，lr 1e-6、β 0.1） | 100 | 25 | 2 | 三级 | — | — | **79%** | **−2.0%** |
| 7B | SFT（DeepSeek API 500 条） | 500 | 2 epochs | — | 交叉熵 | — | — | **71%** | **−10.0%** |
| 7B | SFT → GRPO（三级） | 500 + 100 | SFT 2 epochs；GRPO 25/50 | 2 | 三级 | — | — | **78%** | **+7.0%（vs SFT）** |
| 7B | CSC-SQL（零样本参考） | — | — | — | — | — | — | **73%** | **−8.0%** |
| 7B | XiYanSQL（零样本参考） | — | — | — | — | — | — | **44%** | **−37.0%（格式不匹配）** |
| 7B | OmniSQL（零样本参考） | — | — | — | — | — | — | **66%** | **−15.0%** |

**汇报要点：**

- GRPO 提升了 3B 模型：执行匹配 37% → 39%（+2%），执行成功 59% → 64%（+5%），收敛稳定（KL 下降、梯度范数下降、奖励标准差下降）。
- 二值奖励下扩大数据和步数会过拟合：3B 在 1000 条上掉到 **33%**（低于基线）。
- 奖励稀疏是诊断结论，三级奖励（1.0/0.1/0.0）已实现并在 7B 上验证：止住了崩塌，但 100 条样本下没有超过基线（77% 到 79%）。
- 模型容量主导：7B 零样本 **81%** 对比 3B 训练后的 39%。任何 7B 微调（RL 或 SFT）都没有超过 81% 基线，瓶颈是数据量和奖励信号密度，不是优化器设置。
- SFT 冷启动是净损失（−10%），RL 能修复大部分（+7%），但流水线总体不优于零样本。
- 外部模型（CSC-SQL 73%、OmniSQL 66%、XiYanSQL 44%）在统一评测下都低于原版 7B 零样本（81%）。
- 流水线在单卡上完全可复现。官方 Spider 评估器集成，以及在更大数据规模上验证三级奖励和列级部分分，是立即可做的下一步。

---

*附录：关键文件。src/train_reasoning_grpo.py（GRPO + 奖励工厂）、src/train_sft.py（SFT 冷启动）、src/generate_api_data.py（DeepSeek API 数据）、src/evaluate_after_grpo.py（训练后评估）、src/run_spider_baseline.py（基线评估）、scripts/{train_3b_aggressive,train_7b,exp_reward,exp_grpo_tuned,sft_grpo_pipeline,eval_checkpoints,eval_7b_ckpts,eval_all_models,eval_csc}.slurm。*
