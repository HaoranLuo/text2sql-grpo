# 进度报告：面向 Text-to-SQL 的 7B 推理生成器 GRPO 微调

> **日期**：2026-08-03
> **作者**：jiahuiwang24
> **平台**：西交利物浦大学（XJTLU）高性能计算平台（SLURM，A40 分区）
> **范围**：第二阶段实验（E5–E15），在 3B 阶段完成端到端流水线验证后，将方案迁移至 7B 模型，系统验证奖励函数演进、SFT 冷启动与外部模型对比
> **结论先行**：7B 零样本基线 **81%** 是当前项目最佳结果，全部 11 项 7B 微调/外部模型实验（最高 79%）均未超越该基线

---

## 1. 项目背景与 7B 模型引入

### 1.1 研究目标

本项目研究强化学习（GRPO，组相对策略优化）能否提升开源模型作为"推理生成智能体"（Reasoning Generator Agent）的 Text-to-SQL 性能。该智能体先逐步生成推理轨迹，再输出最终 SQL 查询。项目动机源于 OmniSQL-32B 研究方向：大模型训练和迭代的成本高昂。第一阶段先在 3B 模型上把完整流程跑通（数据准备 → 智能体构建 → GRPO 训练 → 评估），随后将方案稳妥迁移到更大模型（7B → 32B）。

**核心问题**：在单张 GPU 和极小训练预算下，对 Qwen2.5-Coder 基座模型做 GRPO + LoRA 微调，能否提升其在 Spider Text-to-SQL 基准上的执行准确率？

### 1.2 3B 阶段的关键结论与瓶颈定位

3B 阶段（E1–E3）确立了方案的天花板，并定位了瓶颈所在：

| 编号 | 实验 | 解析 | 执行成功 | **执行匹配** | 对比基线 |
|---|---:|---:|---:|---:|
| E1 | 3B 零样本基线 | 98% | 59% | **37%** | — |
| E2 | 3B + GRPO 40 步（100 条 / 2 候选 / 二值） | 98% | 64% | **39%** | **+2.0%** |
| E3 | 3B + GRPO 100 步（1000 条 / 4 候选 / 二值） | — | — | **33%** | **−4.0%（过拟合）** |

三个结论性发现：

1. **GRPO 有效但幅度有限**：E2 表明 GRPO 先教会模型生成*语法合法*的 SQL（执行成功率 +5.0%，59% → 64%），再教会生成*正确*的 SQL（匹配率 +2.0%，37% → 39%）。训练动态健康：KL 0.0014 → 0.0004，奖励标准差 0.42 → 0.14，梯度范数 27.1 → 0.013。
2. **二值奖励下扩大数据与步数导致过拟合**：E3 在 1000 条样本、4 个候选、≥100 步下掉到 33%，低于 37% 基线。0/1 奖励无法区分"可执行但结果错误"（通向正确的良好一步）与"语法错误"（死胡同），信号稀疏，策略只能记忆训练样本。
3. **瓶颈是奖励稀疏与模型容量，而非 RL 机制本身**：优化器工作正常（KL、梯度、奖励方差均收敛），问题在于信号密度与容量上限。

### 1.3 迁移到 7B 的理由

1. **容量是硬约束**：Qwen2.5-Coder-7B-Instruct 零样本执行匹配率即达 **81%**，是 3B 训练后结果（39%）的 2 倍以上。模型规模压倒了 3B 阶段尝试的各种技巧，选择更大的基座模型是杠杆最高的改动。
2. **3B 验证了完整流水线**：数据管线、提示词、GRPO 训练器、评估协议均已在 3B 上打通并稳定运行，迁移成本低。
3. **方向一致性**：项目目标本就是"用较小语言模型替代大模型并验证方案，再向 7B → 32B 演进"；文献（FINER-SQL：3B + GRPO + 记忆奖励 → Spider 85%，单 GPU 可复现）也显示小模型 + 好奖励存在潜力上限，7B 是验证"奖励重设计 + 数据规模"假设的合理中间规模。

### 1.4 7B 阶段实验设计逻辑（六步）

1. **确立 7B 天花板**：零样本基线（E5，81%）。
2. **验证二值奖励过拟合诊断与模型规模无关**：二值奖励 GRPO（E6）。
3. **A/B 测试奖励修正**：受控条件下三级奖励 vs 二值奖励、25 步 vs 50 步（E7、E8）。
4. **抑制优化漂移**：降低学习率（1e-6）、增强 KL 惩罚（β=0.1）（E9）。
5. **SFT 冷启动**：GRPO 前模仿 DeepSeek 教师模型推理轨迹（E10 → E11/E12）。
6. **外部已发表模型对比**：CSC-SQL、XiYanSQL、OmniSQL 零样本作为外部参考点（E13–E15）。

研究计划曾预期"7B + 三级奖励 + 40–50 步 → 81% → 85–88%"，实际最高仅 79%，未达预期，原因分析见第 7、8 节。

---

## 2. 技术配置

### 2.1 硬件与运行环境

| 组件 | 取值 |
|---|---|
| HPC 平台 | XJTLU HPC（login.hpc.xjtlu.edu.cn），SLURM |
| GPU | NVIDIA A40（48 GB），分区 `aiaca40`（QoS `1a40`） |
| Python | 3.10.20（conda 环境 `reasoning3b`） |
| PyTorch | 2.5.1+cu124 |
| Transformers | 4.48.3 |
| TRL | 0.15.2（`GRPOTrainer`、`GRPOConfig`） |
| PEFT | 0.14.0 |
| Accelerate / Datasets | 1.3.0 / 3.2.0 |

### 2.2 模型

- **基座模型**：Qwen2.5-Coder-7B-Instruct（7B 参数，面向代码与推理优化），bf16 本地加载。
- **训练方法**：GRPO + LoRA。LoRA 配置：**r=16、alpha=32、dropout=0.05**，目标模块为全部 7 个线性投影（q_proj、k_proj、v_proj、o_proj、gate_proj、up_proj、down_proj），可训练参数约占 7B 的 0.3%。

### 2.3 数据集

- **Spider**（`data/spider_data/`）：`train_spider.json`（训练）、`dev.json`（评估）、`tables.json`（表结构元数据）。
- **训练集**：`train_spider.json` 前 100 条（SFT 使用 DeepSeek API 生成的 500 条，见 E10）。
- **评估集**：`dev.json` 前 100 条，所有实验完全相同，保证可比。
- 每条样本包含：prompt（用智能体自身的 11 条指令提示词构建，保证训练与推理一致）、question、query（金标准 SQL，**仅用于奖励计算，生成过程中不展示给模型**）、db_id、ddl（从 sqlite_master 提取，缺失时回退 tables.json）。

### 2.4 GRPO 训练配置（7B 标准）

| 参数 | 取值 | 理由 |
|---|---|---|
| 训练样本 | 100 | 小预算验证 |
| 每组生成数（组大小） | 2 | 适配 48 GB 显存 |
| 学习率 | 5e-6 | LoRA 微调标准值（调参实验用 1e-6） |
| KL 惩罚系数 β | 0.04 | 使策略贴近参考模型（调参实验用 0.1） |
| 采样温度 | 0.7 | 保证候选多样性 |
| 最大提示长度 | 1536 tokens | |
| 最大补全长度 | 256 tokens | SQL 与推理预算 |
| 梯度检查点 | 开启 | 节省显存 |
| 精度 | bf16 | |
| 批大小 | `num_generations` 的整数倍 | TRL 约束 |
| 训练步数 | 25 / 50（按实验） | |

算法实现：TRL 0.15.2 `GRPOTrainer`（无 critic，组内相对优势，KL 正则）。

### 2.5 评估协议与指标

| 指标 | 定义 |
|---|---|
| **解析成功率（Parse）** | 能从预测中提取出 SQL 的比例（正则代码块解析器） |
| **执行成功率（Exec）** | 预测 SQL 能在 SQLite 上无错误执行的比例 |
| **执行匹配率（Match）** | 预测 SQL 结果行与金标准 SQL 结果行完全一致的比例（**主指标**） |
| SQL 字符串匹配率 | 归一化字符串相等（3B 阶段为 1%，近似下限，7B 阶段未单独追踪） |

评估协议要点：① 同一份 100 条开发集切片；② 金标准 SQL 从不展示给模型；③ SQLite 只读执行并带安全检查；④ 排序感知——金标准 SQL 含 ORDER BY 时比较保留行顺序；⑤ 主指标为自定义执行匹配，**非官方 Spider 指标**（官方 test-suite-sql-eval 集成已列入计划）。

### 2.6 奖励类型

| 类型 | 打分 | 用途 |
|---|---|---|
| `binary`（二值） | 行匹配 1.0，否则 0.0 | 原设计；过于稀疏 |
| `three_level`（三级） | 行匹配 1.0；可执行但错误 0.1；不可执行 0.0 | Agentar-Scale-SQL 方案；给予部分得分 |

---

## 3. 全部 7B 实验明细

> **编号说明**：本报告采用 E5–E15 编号，衔接 3B 阶段的 E1–E3，与 EXPERIMENT_LOG.md 中的内部编号（E4–E14，其中 E4 为 7B 基线）存在 +1 偏移，两者一一对应。日期为尽力记录（SLURM 日志保存在 HPC 上，本地文件仅记录最终报告）。

### 3.1 实验总表（Spider dev-100 执行匹配率）

| 编号 | 实验 | 解析 | 执行成功 | **匹配** | Δ（对比 E5） | 状态 |
|---|---|---|---|---|---|---|
| E5 | 7B 零样本基线 | ~100% | 97% | **81%** | — | ✅ 当前最佳 |
| E6 | 7B + GRPO 二值奖励 25 步 | — | — | **79%** | −2.0 | ✅ 过拟合 |
| E7 | 7B + GRPO 三级奖励 25 步 | — | — | **77%** | −4.0 | ✅ |
| E8 | 7B + GRPO 三级奖励 50 步 | — | — | **79%** | −2.0 | ✅ |
| E9 | 7B + GRPO 调参（lr=1e-6, β=0.1, 25 步） | — | — | **79%** | −2.0 | ✅ |
| E10 | 7B + SFT（DeepSeek API 500 条） | — | — | **71%** | −10.0 | ✅ |
| E11 | 7B + SFT + GRPO 25 步 | — | — | **78%** | +7.0（vs E10） | ✅ |
| E12 | 7B + SFT + GRPO 50 步 | — | — | **78%** | +7.0（vs E10） | ✅ |
| E13 | CSC-SQL-7B（预训练，零样本） | — | — | **73%** | −8.0 | ✅ |
| E14 | XiYanSQL-7B（零样本） | — | — | **44%** | −37.0 | ✅ 格式不匹配 |
| E15 | OmniSQL-7B（零样本） | — | — | **66%** | −15.0 | ✅ |

*注：E6–E15 仅记录了主指标（执行匹配率），解析/执行率未单独存档，以"—"表示。*

**一句话总结：没有任何微调超过 7B 零样本基线（81%）。所有 RL/SFT 实验要么退化，要么只恢复到基线附近——小数据（100 条）、二值奖励与 SFT 数据质量是疑似根因。**

---

### 3.2 E5 — 7B 零样本基线（约 2026-08-02）

- **模型**：Qwen2.5-Coder-7B-Instruct（零样本，无训练）
- **配置**：— | **GPU**：A40

| 指标 | 值 | 对比 3B 基线（E1） |
|---|---:|---:|
| 解析 | ~100%（100/100） | +2.0 |
| 执行成功 | 97%（97/100） | **+38.0** |
| **执行匹配** | **81%（81/100）** | **+44.0** |

- **分析**：**规模主导一切。** 7B 零样本（81%）比 3B 零样本（37%）高 44 个点，超过此前测得的任何微调增益。7B 生成近乎合法的 SQL（97% 可执行），剩余 16 个点的差距在**语义层**（结果正确性：错表、错列、错 JOIN），不在语法层——这正是 RL 应解决的"硬推理"部分。该模型至今仍是本项目最佳模型。
- **状态**：✅ completed 🏆 · 证据：`scripts/eval_all_models.slurm`（头部数值）、`scripts/train_7b.slurm`

---

### 3.3 E6 — 7B + GRPO 二值奖励 25 步（约 2026-08-02）

- **模型**：Qwen2.5-Coder-7B-Instruct + LoRA
- **配置**：25 步 · 100 条样本 · 2 候选/组 · **二值**奖励 · lr 5e-6 · β 0.04 · 温度 0.7 | **GPU**：A40

| 指标 | 训练前（E5） | 训练后 | Δ |
|---|---:|---:|---:|
| 解析 | ~100% | — | — |
| 执行成功 | 97% | — | — |
| **执行匹配** | 81% | **79%** | **−2.0** |

- **分析**：**GRPO 伤害了强基座模型。** 与 E3 相同的二值奖励过拟合模式，这次发生在本身已经很好的模型上：100 条样本上的 RL 把策略推离其强先验，未带来正确性净增益。证实"二值奖励过拟合"诊断与模型规模无关。
- **状态**：✅ completed（过拟合）· 证据：`RESEARCH_PLAN.md` §1、`scripts/train_7b.slurm`

> [^1] 步数记录存在不一致：EXPERIMENT_LOG.md 将 7B 二值实验记录为 100 步（"E5 — 7B + GRPO 100-step, binary"），3B_PROGRESS_REPORT.md §7.2 记录为 25 步。本报告遵循 3B_PROGRESS_REPORT.md 与既有编号体系记录为 25 步，结果（79%）不受影响。

---

### 3.4 E7 — 7B + GRPO 三级奖励 25 步（约 2026-08-02）

- **模型**：Qwen2.5-Coder-7B-Instruct + LoRA
- **配置**：25 步（checkpoint-25）· 100 条样本 · 2 候选/组 · **three_level** 奖励（1.0/0.1/0.0）· lr 5e-6 · β 0.04 · 温度 0.7 | **GPU**：A40

| 指标 | 训练前（E5） | 训练后 | Δ |
|---|---:|---:|---:|
| 解析 | ~100% | — | — |
| 执行成功 | 97% | — | — |
| **执行匹配** | 81% | **77%** | **−4.0** |

- **分析**：三级奖励（Agentar-Scale-SQL 的 P0 方案）**没有在 100 条样本上拯救 7B**。短训练欠矫正；数据极小时，密集信号本身不足以带来提升。
- **状态**：✅ completed · 证据：`scripts/exp_reward.slurm`（Group A checkpoint-25 评估）

---

### 3.5 E8 — 7B + GRPO 三级奖励 50 步（约 2026-08-02）

- **模型**：Qwen2.5-Coder-7B-Instruct + LoRA
- **配置**：50 步 · 100 条样本 · 2 候选/组 · **three_level** 奖励 · lr 5e-6 · β 0.04 · 温度 0.7 | **GPU**：A40

| 指标 | 训练前（E5） | 训练后 | Δ |
|---|---:|---:|---:|
| 解析 | ~100% | — | — |
| 执行成功 | 97% | — | — |
| **执行匹配** | 81% | **79%** | **−2.0** |

- **分析**：50 步好于 25 步（79% > 77%），但仍低于基线。同级步数下三级奖励仅比二值奖励有微幅恢复（E6 二值同为 79%），**从未超过 81%**。
- **状态**：✅ completed · 证据：`scripts/exp_reward.slurm`（Group A 最终）

---

### 3.6 E9 — 7B + GRPO 调参（lr=1e-6、β=0.1）（约 2026-08-02 → 08-03）

- **模型**：Qwen2.5-Coder-7B-Instruct + LoRA
- **配置**：25 步 · 100 条样本 · 2 候选/组 · **three_level** 奖励 · **lr 1e-6（降为 1/5）· β 0.1（提高 2.5 倍）** · 温度 0.7 | **GPU**：A40

| 指标 | 训练前（E5） | 训练后 | Δ |
|---|---:|---:|---:|
| 解析 | ~100% | — | — |
| 执行成功 | 97% | — | — |
| **执行匹配** | 81% | **79%** | **−2.0** |

- **分析**：更强的 KL 正则 + 更小的学习率在 100 条样本上没有任何帮助，与未调参的 50 步同为 79%。**瓶颈是数据量与奖励信号密度，不是优化不稳定性。**
- **状态**：✅ completed · 证据：`scripts/exp_grpo_tuned.slurm`

---

### 3.7 E10 — 7B + SFT 冷启动（DeepSeek API 500 条）（约 2026-08-03）

- **模型**：Qwen2.5-Coder-7B-Instruct + LoRA，SFT
- **配置**：500 条 API 生成样本（`data/api_sft_data.json`，DeepSeek V4 Flash API，温度 0.3，max_tokens 1024，`src/generate_api_data.py`）· 2 epochs · **lr 2e-5** · batch 1 · 梯度累积 4 · 最大长度 2048 · bf16 · LoRA r16 | **GPU**：A40

| 指标 | 训练前（E5） | 训练后 | Δ |
|---|---:|---:|---:|
| 解析 | ~100% | — | — |
| 执行成功 | 97% | — | — |
| **执行匹配** | 81% | **71%** | **−10.0** |

- **分析**：**对 API 生成数据的 SFT 主动伤害性能。** 模仿 DeepSeek 的推理/SQL 格式把策略推离 7B 自身的强项；500 条混合质量的 API 样本劣于零样本。单独 SFT 不是可行的冷启动（详见第 5 节）。
- **状态**：✅ completed · 证据：`scripts/sft_grpo_pipeline.slurm` Step 1、`src/train_sft.py`、`scripts/run_api_gen.sh`

---

### 3.8 E11 — 7B + SFT + GRPO 25 步（约 2026-08-03）

- **模型**：Qwen2.5-Coder-7B-Instruct + SFT LoRA + GRPO LoRA
- **配置**：在 SFT 模型上做 GRPO · 25 步（checkpoint-25）· 100 条样本 · 2 候选/组 · **three_level** 奖励 · lr 5e-6 · β 0.04 | **GPU**：A40

| 指标 | SFT 后（E10） | GRPO 后 | Δ |
|---|---:|---:|---:|
| **执行匹配** | 71% | **78%** | **+7.0** |

- **分析**：GRPO 在 SFT 后恢复 **+7.0 个点**（71% → 78%），几乎回到零样本水平（81%）但仍差 3 个点。**RL 作为修复机制有效；SFT 本身才是损伤来源。**
- **状态**：✅ completed · 证据：`scripts/sft_grpo_pipeline.slurm`、`scripts/eval_all_models.slurm`

---

### 3.9 E12 — 7B + SFT + GRPO 50 步（约 2026-08-03）

- **模型**：Qwen2.5-Coder-7B-Instruct + SFT LoRA + GRPO LoRA
- **配置**：在 SFT 模型上做 GRPO · 50 步（最终）· 100 条样本 · 2 候选/组 · **three_level** 奖励 · lr 5e-6 · β 0.04 | **GPU**：A40

| 指标 | SFT 后（E10） | GRPO 后 | Δ |
|---|---:|---:|---:|
| **执行匹配** | 71% | **78%** | **+7.0** |

- **分析**：25 步与 50 步结果完全相同（78%），出现**平台期**。SFT+GRPO 流水线不优于直接零样本；在此数据规模下，流水线复杂度没有换来收益。
- **状态**：✅ completed · 证据：`scripts/sft_grpo_pipeline.slurm`、`scripts/eval_all_models.slurm`

---

### 3.10 E13 — CSC-SQL-7B 预训练模型（零样本）（约 2026-08-03）

- **模型**：`cycloneboy/CscSQL-Grpo-Qwen2.5-Coder-7B-Instruct`（第三方 GRPO 预训练，无进一步训练）
- **配置**：纯评估，使用我们自己的 11 条指令提示词 | **GPU**：A40

| 指标 | 值 | 对比 E5 |
|---|---:|---:|
| **执行匹配** | **73%** | **−8.0** |

- **分析**：第三方 GRPO 调优的 7B 在我们的提示词与评估条件下仍输给原版 7B 零样本（73% vs 81%）。可能原因：① 其训练分布与我们的评估分布不同；② 我们的提示词模板未触发其调优行为。**在自定义评测框架下，外部检查点不是越过基线的捷径。**
- **状态**：✅ completed · 证据：`scripts/eval_csc.slurm`、`tmp/download_csc.py`

---

### 3.11 E14 — XiYanSQL-7B（零样本）（约 2026-08-03）

- **模型**：`XGenerationLab/XiYanSQL-QwenCoder-7B-2502`（无训练）
- **配置**：纯评估，使用我们的 11 条指令提示词 | **GPU**：A40

| 指标 | 值 | 对比 E5 |
|---|---:|---:|
| **执行匹配** | **44%** | **−37.0** |

- **分析**：**格式不匹配。** XiYanSQL 使用自带的 schema 链接与输出格式训练，输出额外标记而非我们的 ```sql 代码块，导致解析器在大多数输出上失败。**44% 主要反映解析失败，不代表模型质量。** 公平对比前需要做提示词与格式适配（详见第 6 节）。
- **状态**：✅ completed（格式不匹配，结果不可比）· 证据：`scripts/eval_all_models.slurm`、`tmp/dl_both.py`

---

### 3.12 E15 — OmniSQL-7B（零样本）（约 2026-08-03）

- **模型**：`seeklhy/OmniSQL-7B`（无训练）
- **配置**：纯评估，使用我们的 11 条指令提示词 | **GPU**：A40

| 指标 | 值 | 对比 E5 |
|---|---:|---:|
| **执行匹配** | **66%** | **−15.0** |

- **分析**：OmniSQL（66%）在我们的评测框架下远好于 XiYanSQL（44%），但仍低于原版 Qwen 7B 零样本（81%）。与 E13 结论一致：**外部 Text-to-SQL 检查点在我们的流水线里输给基座模型。**
- **状态**：✅ completed · 证据：`scripts/eval_all_models.slurm`、`tmp/dl_both.py`

---

### 3.13 7B 实验超参数速查表

| 实验 | 模型 | 步数 | 训练数据 | 生成/组 | 奖励 | lr | β | 温度 | GPU |
|---|---|---|---|---|---|---|---|---|---|
| E5 | 7B | — | — | — | — | — | — | — | A40 |
| E6 | 7B | 25 | 100 | 2 | 二值 | 5e-6 | 0.04 | 0.7 | A40 |
| E7 | 7B | 25 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |
| E8 | 7B | 50 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |
| E9 | 7B | 25 | 100 | 2 | 三级 | **1e-6** | **0.1** | 0.7 | A40 |
| E10（SFT） | 7B | 2 epochs | 500（API） | — | 交叉熵 | 2e-5 | — | — | A40 |
| E11/E12 | 7B+SFT | 25 / 50 | 100 | 2 | 三级 | 5e-6 | 0.04 | 0.7 | A40 |
| E13 | CSC-SQL-7B | — | — | — | — | — | — | — | A40 |
| E14 | XiYanSQL-7B | — | — | — | — | — | — | — | A40 |
| E15 | OmniSQL-7B | — | — | — | — | — | — | — | A40 |

通用配置：LoRA r=16 / alpha=32 / dropout=0.05 / 7 个模块；TRL 0.15.2 `GRPOTrainer`；最大提示 1536；最大生成 256；梯度检查点；bf16。

---

## 4. 奖励函数演进

### 4.1 二值奖励及其缺陷（原设计）

第三阶段规格中的二值奖励简单且可解释：

```python
# 二值奖励（train_reasoning_grpo.py 原版 reward_func）
if rows_match:
    reward = 1.0
else:
    reward = 0.0
```

**缺陷**：0/1 信号过于稀疏。奖励无法区分"可执行但结果错误"（通向正确的良好一步）与"语法错误"（死胡同）。在 N 个候选很少正确、奖励几乎全 0 的情况下，组相对优势退化为对噪声的过优化——策略被推向组内均值局部所奖励的方向，额外数据放大的是奖励黑客和记忆，而不是泛化。证据：

- E3（3B，1000 条 × 4 候选 × 100 步，二值）：33%，低于 37% 基线（过拟合崩塌）。
- E6（7B，100 条 × 2 候选 × 25 步，二值）：79%，低于 81% 基线（−2.0）。

### 4.2 三级奖励的设计（Agentar-Scale-SQL 方案）

文献证据（Agentar-Scale-SQL，蚂蚁集团，BIRD 第一名 81.67%）显示三级奖励是杠杆最高的修正。一行改动即把稀疏的二值奖励变成分级奖励，让"可执行但错误"的候选也能给策略提供学习信号：

```python
# 三级奖励（--reward-type three_level 实现，当前默认）
if rows_match:
    reward = 1.0
elif sql_is_executable:
    reward = 0.1   # ← 关键：可执行但结果错也有信号
else:
    reward = 0.0
```

评分语义：

| 层级 | 得分 | 含义 |
|---|---:|---|
| 完全正确 | 1.0 | 结果行与金标准一致（ORDER BY 感知） |
| 部分得分 | 0.1 | SQL 可执行但结果错误（缺失的中间信号） |
| 不可执行 | 0.0 | 语法错误或执行失败 |

### 4.3 演进证据与结论

| 实验 | 奖励 | 步数 | 匹配率 | 结论 |
|---|---|---|---|---|
| E3 | 二值 | 100 | 33% | 崩塌式过拟合 |
| E6 | 二值 | 25 | 79% | 低于基线 −2.0 |
| E7 | 三级 | 25 | 77% | 低于基线 −4.0 |
| E8 | 三级 | 50 | 79% | 低于基线 −2.0 |
| E9 | 三级（调参） | 25 | 79% | 与 E8 相同 |

**结论**：三级奖励**消除了二值奖励式的崩塌过拟合**（E7–E9 稳定在 77–79%，未再出现 33% 式崩溃），但**没有转化为超越基线的收益**。在 100 条样本的规模下，密集信号本身不足以驱动超越——数据量是下一步的验证焦点。

---

## 5. SFT 实验分析

### 5.1 SFT 冷启动的设计动机

在 RL 之前，策略在温度 0.7 下生成的补全质量低；稀疏奖励下（N 个候选中很少正确）从零开始的 GRPO 效率低且易奖励黑客。SFT 冷启动遵循推理 LLM 文献中"先模仿后 RL"的配方（DeepSeek-R1 流程、CSC-SQL、FINER-SQL）：

1. 用更强的教师模型 **DeepSeek V4 Flash API**（温度 0.3，max_tokens 1024）为 500 条 Spider 训练样本生成推理与 SQL（`src/generate_api_data.py` → `data/api_sft_data.json`）；
2. 在这些轨迹上对 7B 做 SFT（LoRA r=16，2 epochs，lr 2e-5，batch 1，梯度累积 4，max len 2048，bf16，`src/train_sft.py`）；
3. 再在 SFT 模型上做 GRPO（E11/E12）。

预期：SFT 先把策略预热，使 GRPO 从正确 SQL 更常见的分布出发，组相对优势信号更密，KL 惩罚的破坏更小。

### 5.2 实际结果与失败原因

| 实验 | 阶段 | 匹配率 | Δ |
|---|---|---:|---:|
| E5 | 7B 零样本 | 81% | — |
| E10 | 7B + SFT（500 条 API） | 71% | **−10.0** |
| E11/E12 | 7B + SFT + GRPO（25/50 步） | 78% | **+7.0（vs E10）** |

失败原因分析：

1. **没有正确性过滤**：原始 API 补全未针对金标准 SQL 做执行匹配过滤，混合了正确与错误的轨迹，注入噪声。KL 惩罚只能与其对抗，无法清除。
2. **分布错位**：DeepSeek 的推理/SQL 格式与 Qwen2.5-Coder-7B 的原生输出分布不同，SFT 强迫 7B 模仿异质格式，把策略推离其强项（零样本即 81% 的能力被稀释）。
3. **数据量小且混合**：500 条样本、2 epochs 对分布偏移的矫正足以造成伤害（−10 点），却不足以建立新的强分布。

### 5.3 结论

- **RL 是修复工具，不是冷启动的替代品**：GRPO 从 SFT 的 71% 恢复到 78%（+7.0），证明 RL 能修复大部分损伤，但"先模仿后 RL"流水线在此数据规模下收益有限，整体不优于直接零样本。
- **SFT 数据质量是前提**：今后应在模仿训练前用执行匹配过滤 SFT 语料（见第 9 节）。

---

## 6. 外部模型对比

对三个已发布的 Text-to-SQL 7B 模型在**统一评估协议**（我们的 11 条指令提示词、Spider dev-100、自定义执行匹配）下做零样本对比，作为外部参考点：

| 模型（HF 仓库） | 匹配率 | vs E5 | 备注 |
|---|---|---:|---:|
| `cycloneboy/CscSQL-Grpo-Qwen2.5-Coder-7B-Instruct`（CSC-SQL） | **73%** | −8.0 | 第三方 GRPO 预训练 |
| `XGenerationLab/XiYanSQL-QwenCoder-7B-2502`（XiYanSQL） | **44%** | −37.0 | 格式不匹配，不可比 |
| `seeklhy/OmniSQL-7B`（OmniSQL） | **66%** | −15.0 | — |
| **Qwen2.5-Coder-7B-Instruct（E5，无任何 SQL 专项训练）** | **81%** | — | 🏆 |

### 6.1 CSC-SQL-7B（73%，−8.0）

GRPO 预训练的 7B 模型在自家提示词下输给原版零样本。两种解释：① 训练分布（数据集、提示词）与我们的评估分布不一致；② 我们的提示词模板未触发其调优行为。**外部检查点不是越过基线的捷径**——任何引入的模型都必须在本项目的提示词与评估协议下重新验证。

### 6.2 XiYanSQL-7B（44%，−37.0）：格式不匹配分析

XiYanSQL 用自带的 schema 链接 + 输出格式训练，其输出包含额外标记（schema 链接结果、结构化标记等）而非直接的 ```sql 代码块。我们的正则解析器在大多数输出上失败，44% 主要反映**解析失败率**而非模型能力。该结果不可作为模型质量评判依据；公平对比需两步：

1. 适配解析器以识别其输出格式（或改造提示词要求其输出 ```sql 块）；
2. 再以统一协议重测。

### 6.3 OmniSQL-7B（66%，−15.0）

与 XiYanSQL 同源下载脚本（`tmp/dl_both.py`）评估，格式兼容性明显更好（66% vs 44%），但语义能力仍低于原版 Qwen 7B 零样本（81%）。与 CSC-SQL 结论一致。

### 6.4 对比总结

1. 三个外部 SQL 专项模型（73% / 44% / 66%）在统一评测下**全部输给原版 Qwen2.5-Coder-7B 零样本（81%）**。
2. XiYanSQL 的差距主要是提示词/格式失配，需适配后重测才能下结论；CSC-SQL 与 OmniSQL 的差距则更可能反映真实能力差异或分布差异。
3. 结论强化了"基座模型容量主导"的判断：在自定义流水线下，外部微调检查点无法替代对基座模型的自主调优。

---

## 7. 关键发现：为什么没有微调超过零样本 81%

### 7.1 规模压倒了微调

3B → 7B 零样本跃升 **+44.0 个点**（37% → 81%），超过所有训练增益的总和。模型选择是主导杠杆，训练技巧（奖励工程、组大小、步数、正则）在容量面前是二阶因素。

### 7.2 二值奖励导致过拟合，三级奖励止住崩塌但无超越

二值奖励实验（E3：33%、E6：79%）均退到基线以下；三级奖励（E7–E9：77–79%）消除了崩塌，但 100 条样本下从未超过 81%。奖励密化解决了"学习崩溃"，没有解决"超越起点"。

### 7.3 100 条样本上的 RL 对强基座模型是净负

所有 7B GRPO 运行都落在 **77%–79%**，低于 81% 基线。E9 调参（lr 1e-6、β 0.1）结果不变（79%）证明**瓶颈是数据量与奖励信号密度，而非优化器设置或训练不稳定性**。

### 7.4 RL 是修复工具，不是冷启动

SFT 造成 −10.0（E10，71%），RL 修复 +7.0（E11/E12，78%），但流水线总体不优于零样本（81%）。"先模仿后 RL"在此数据规模下收益有限。

### 7.5 外部模型不构成捷径

CSC-SQL 73%、OmniSQL 66%、XiYanSQL 44%（格式失配）在统一协议下均低于 81% 基线。

### 7.6 语义差距是剩余空间

7B 执行率 97% vs 匹配率 81%，中间 **16 个点**是语义错误（错表、错列、错 JOIN），这正是 RL 设计上应解决的"硬推理"部分——但 100 条样本的奖励信号不足以教会它。

### 7.7 训练动态始终健康

即使指标在降（E3、E6），KL 下降、奖励标准差下降、梯度范数趋近 0——**优化器没有故障，是奖励信号和数据出了问题**。这排除了工程实现层面的干扰，将矛盾集中于数据规模与奖励设计。

---

## 8. 经验教训

### 8.1 模型规模远大于训练技巧

零样本 7B（81%）击败完整 GRPO 训练的 3B（39%），超过 2 倍。在 Text-to-SQL 上，基座容量压倒奖励工程、组大小和步数。3B 训练时间对流水线验证是值得的，但容量是硬约束。7B 实验是对的方向，后续应转向数据规模与奖励精化。

### 8.2 二值奖励导致过拟合——奖励设计是第一优先

- 3B（1000 条，33%）与 7B（二值 25 步，79%）的二值奖励实验都退化到基线以下。
- 0/1 奖励没有梯度区分"可执行但错误"与"不可解析"，策略只能过优化组内相对噪声。
- 三级奖励（1.0/0.1/0.0）是单行代码里杠杆最高的修正，已成为默认（`--reward-type three_level`）；但 100 条样本下它只止住了崩塌，没有超过基线，**需要在更大的数据上验证**。
- 过拟合不是训练不稳定：40 步实验中 KL 0.0014 → 0.0004（无漂移）、奖励标准差 0.42 → 0.14、梯度范数 27.1 → 0.013——是奖励设计与容量的问题。

### 8.3 SFT 冷启动需要比原始 API 轨迹更高质量的数据

SFT 设计（500 条 DeepSeek API 轨迹、2 epochs）遵循"先模仿后 RL"配方，但**原始 API 补全没有针对金标准 SQL 做正确性过滤**，实验证实造成 −10.0 点伤害（E10），RL 修复大部分（+7.0，E11/E12）。今后应在模仿训练前用执行匹配过滤 SFT 语料；混合正确与错误轨迹会注入噪声，KL 惩罚只能与其对抗。

### 8.4 评估方法

- **字符串匹配在这里没有用**：3B 阶段仅 1/100（1%）。结果相同的不同 SQL 写法（如 `age > 20` 与 `20 < age`）永远不会字符串匹配。执行匹配才是正确的主指标。
- **自定义 `custom_execution_match`**（行集相等、ORDER BY 感知）是快速的训练前基线，明确不是官方 Spider 指标；官方数字需要 `test-suite-sql-eval` 评估器（全量开发集上的精确匹配 + 执行准确率），列入最终报告计划。
- 评估协议质量要点：所有实验共用同一份 100 条开发集切片；金标准 SQL 从不展示给模型；SQLite 只读执行并带安全检查。

### 8.5 工程经验（HPC / TRL）

- TRL 0.15.2 改了 API 名：`reward_funcs`（不是 `reward_functions`）、`processing_class`（不是 `tokenizer`）。
- `per_device_train_batch_size` 必须是 `num_generations` 的整数倍。
- 显存不足时的缓解：梯度检查点 + 截断序列长度 + 2 个候选。
- 右填充下批推理解析率掉到 40%，改用**左填充**。
- 访问 GPU 节点需先 `salloc` 再 `srun --pty bash`；`gpudebug` 分区上限 1 小时；正确的 QoS 是 `1a40`（查自 `sacctmgr`）。
- JSON 里的 set 对象要转成 `sorted(list(...))`；进度条写到 stderr（查 `.err` 文件）。

---

## 9. 后续方向

### 9.1 当前状态（2026-08-03）

- ✅ 7B 基线确立：零样本 **81%**，当前已知最佳，任何微调均未超过。
- ✅ 奖励重设计（three_level）实现并在 7B 上完成 A/B 验证（E7/E8），止住崩塌但 100 条下无超越。
- ✅ 调参 GRPO（E9）、SFT 冷启动（E10）与 SFT+GRPO（E11/E12）、外部模型评估（E13–E15）全部完成。
- ✅ 全部 14 个实验（3B 阶段 E1–E3，7B 阶段 E5–E15）已完成，当前无运行中的实验。

### 9.2 按优先级排序的后续计划

#### P0 — 立即可做

| 行动 | 预期收益 | 工作量 |
|---|---|---|
| **三级奖励在更大数据上重试**：训练样本从 100 条扩到 ≥500–1000 条。100 条下三级奖励未超基线（77–79%），数据量是疑似瓶颈 | 验证奖励信号密度假设 | A40 上 2–6 小时 |
| 集成官方 Spider 评估器（`test-suite-sql-eval`：精确匹配 + 执行准确率），在全量开发集上验证 | 论文级指标 | 中 |

#### P1 — 本周（重点：DeepSeek V2 Lite / 高质量 SFT / 部分分奖励）

| 行动 | 预期收益 | 工作量 |
|---|---|---|
| **部分分奖励——列级部分分**（PaVeRL-SQL 风格：`matched_cols / gold_cols`） | +3–5% | 低 |
| 参考 FINER-SQL 实现（3B + GRPO + 记忆奖励 → 85% Spider，单 GPU 可复现），引入**原子操作奖励** | +3–8% | 中 |
| **高质量 SFT 数据**：① 用执行匹配过滤 SFT 语料后再做模仿训练；② 评估更换教师模型（如 DeepSeek V2 Lite，替代/补充当前 V4 Flash API，温度 0.3、max_tokens 1024 的参数可复用），仅保留与金标准执行匹配的轨迹 | 提升 SFT 质量，避免 E10 式 −10 点伤害 | 低 |
| 为 XiYanSQL-7B 与 OmniSQL-7B 适配提示词/格式，做公平对比 | 公平对比 | 低 |

#### P2 — 两周内

| 行动 | 预期收益 | 工作量 |
|---|---|---|
| AutoLink 式 schema 链接（`bge-large-en-v1.5` 列检索，agent 迭代探索 `explore → verify → retrieve → add → stop`） | +3–8% | 中 |
| 多候选生成 + 执行投票（4–8 个候选，锦标赛选拔，可省略 Agentar-Scale-SQL 的 selection model） | +3–5% | 中 |
| 更大的数据切片（500 → 7000 训练行）后再给 RL 下结论 | 验证数据量假设 | 中 |

#### P3 — 长期

| 行动 | 预期收益 | 工作量 |
|---|---|---|
| 过程奖励模型（RuCo-C 式 rubric 批评，对推理过程逐步打分） | +8–12% | 高 |
| Agentic GRPO（AGRO-SQL，POMDP 框架 + 数据工厂自动生成训练数据） | +8–12% | 高 |

### 9.3 预期轨迹

```
3B 零样本          37%
3B GRPO（40 步）    39%   ← 3B 天花板已定位
7B 零样本          81%   ← 容量跃升，当前最佳
7B + 三级奖励      79%   ← 100 条样本下未超基线，数据量是疑点
7B + 更大数据 RL   85%+   ← 待验证（≥500–1000 条 + 三级奖励/列级部分分）
32B/OmniSQL 路径   ~90%   ← 长期
```

---

## 10. 关键指标汇总表

| 模型 | 方法 | 训练集 | 步数 | 候选数 | 奖励 | 解析 | 执行成功 | **执行匹配** | 对比自身基线 |
|---|---|---|---|---|---|---|---|---|---|
| 7B | 零样本基线（E5） | — | — | — | — | ~100% | 97% | **81%** | — |
| 7B | GRPO + LoRA，二值（E6） | 100 | 25 | 2 | 二值 | — | — | **79%** | **−2.0%** |
| 7B | GRPO + LoRA，三级（E7） | 100 | 25 | 2 | 三级 | — | — | **77%** | **−4.0%** |
| 7B | GRPO + LoRA，三级（E8） | 100 | 50 | 2 | 三级 | — | — | **79%** | **−2.0%** |
| 7B | GRPO 调参，三级（E9） | 100 | 25 | 2 | 三级（lr 1e-6、β 0.1） | — | — | **79%** | **−2.0%** |
| 7B | SFT（E10） | 500（API） | 2 epochs | — | 交叉熵 | — | — | **71%** | **−10.0%** |
| 7B | SFT → GRPO（E11/E12） | 500 + 100 | SFT 2 epochs；GRPO 25/50 | 2 | 三级 | — | — | **78%** | **+7.0%（vs SFT）** |
| 7B | CSC-SQL（E13，参考） | — | — | — | — | — | — | **73%** | **−8.0%** |
| 7B | XiYanSQL（E14，参考） | — | — | — | — | — | — | **44%** | **−37.0%（格式不匹配）** |
| 7B | OmniSQL（E15，参考） | — | — | — | — | — | — | **66%** | **−15.0%** |

**汇报要点：**

- 模型容量主导：7B 零样本 **81%** 对比 3B 训练后的 39%。任何 7B 微调（RL 或 SFT）均未超过 81% 基线。
- 二值奖励下扩大数据与步数会过拟合（3B 1000 条掉到 33%）；三级奖励止住崩塌，但 100 条样本下未超基线（77–79%）。
- 调参（lr 1e-6、β 0.1）无效果——瓶颈是数据量与奖励信号密度，不是优化器设置。
- SFT 冷启动是净损失（−10.0%），RL 可修复大部分（+7.0%），但流水线总体不优于零样本。
- 外部模型（CSC-SQL 73%、OmniSQL 66%、XiYanSQL 44%）在统一评测下均低于原版 7B 零样本（81%）；XiYanSQL 差距主要为格式失配。
- 流水线在单卡（A40）上完全可复现。官方 Spider 评估器集成，以及在更大数据规模上验证三级奖励与列级部分分，是立即可做的下一步。

---

## 附录：关键文件

- **训练/评估源码**：`src/train_reasoning_grpo.py`（GRPO + 奖励工厂）、`src/train_sft.py`（SFT 冷启动）、`src/generate_api_data.py`（DeepSeek API 数据）、`src/evaluate_after_grpo.py`（训练后评估）、`src/run_spider_baseline.py`（基线评估）
- **SLURM 脚本**：`scripts/{train_3b_aggressive, train_7b, exp_reward, exp_grpo_tuned, sft_grpo_pipeline, eval_checkpoints, eval_7b_ckpts, eval_all_models, eval_csc, run_api_gen}.slurm/.sh`
- **数据**：`data/api_sft_data.json`（SFT 语料）
- **输出**：`outputs/baseline_pretrain_100/`、`checkpoints/grpo_lora/`、`checkpoints/grpo_3b_aggressive/`、`tmp/download_csc.py`、`tmp/dl_both.py`
- **文档**：`EXPERIMENT_LOG.md`（完整实验日志）、`3B_PROGRESS_REPORT.md`（3B 阶段报告）、`RESEARCH_PLAN.md`（研究计划与文献依据）、`PROJECT_CONTEXT.md`
